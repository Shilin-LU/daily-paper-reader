---
title: Safe Few-Step Generation via Velocity Editing
title_zh: 通过速度编辑实现安全少步生成
authors: "Yujin Choi, Jaehong Yoon"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23267v1"
tags: ["query:vd"]
score: 8.0
evidence: 基于流匹配的少步生成
tldr: "流匹配模型在少步文本到图像生成中表现优异，但现有安全方法因采样步数少和文本编码器局限性而失效。本文提出VESFlow，通过直接编辑速度场引导生成远离不安全概念，且保持良性提示不变。进一步引入风险评分过滤和双向编辑的VESFlow+，在MeanFlow模型上仅用4步即将NudeNet攻击成功率降至6.3%和6.8%，同时保留图像质量。该方法无需训练，为流匹配模型提供了高效的安全保障。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23267v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 501, \"height\": 313, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23267v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 480, \"height\": 319, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23267v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1343, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23267v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1445, \"height\": 459, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23267v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 580, \"height\": 326, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23267v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 789, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23267v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1155, \"height\": 448, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23267v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1432, \"height\": 400, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23267v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 904, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23267v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1312, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23267v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1392, \"height\": 227, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23267v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1419, \"height\": 131, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23267v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1354, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23267v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1400, \"height\": 456, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23267v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1296, \"height\": 392, \"label\": \"Table\"}]"
motivation: 现有安全方法依赖多步迭代或CLIP嵌入操作，难以适配步数极少的流匹配生成，亟需训练免费且高效的轨迹矫正方案。
method: 基于流匹配的速度场特性，用安全条件后验直接编辑速度方向，VESFlow+结合风险过滤避免无效编辑并增加远离不安全方向的分量。
result: "在4步MeanFlow上，Ring-A-Bell和MMA-Diffusion的NudeNet攻击成功率分别降至6.3%和6.8%，良性提示保真度几乎不变。"
conclusion: VESFlow系列为少步流匹配生成提供无需训练的安全移除方法，兼顾高效性与生成质量。
---

## 摘要
流匹配最近成为最先进的文本到图像（T2I）生成领域的一个强大范式，能够以少量采样步数实现高质量生成。随着这些模型越来越多地集成到实际应用中，确保安全和非敏感内容生成已成为关键需求。然而，将安全和概念移除方法适应于这一新生成框架仍然是一个开放挑战。具体来说，先前的方法在很大程度上依赖于跨多个去噪步骤的迭代轨迹引导，或依赖于以CLIP为中心的提示嵌入操作。这些设计假设在基于流匹配的T2I生成中造成了基本瓶颈，因为有限的采样步数限制了迭代校正，而现代上下文感知的文本编码器削弱了嵌入级干预的有效性。在本文中，我们提出了VESFlow，一种专为极少采样步数的流匹配量身定制的无训练安全方法。利用流匹配模型学习边际速度的事实，我们通过安全条件后验直接对速度场进行编辑。VESFlow在保持条件提示不变的情况下，将轨迹引导向安全输出。基于VESFlow在良性提示下输出不变的观察，我们进一步引入了基于风险分数的过滤，绕过速度编辑以减少计算成本，同时保持良性提示生成。基于这种过滤，我们提出了VESFlow+，它是VESFlow的更强变体，不仅向安全方向编辑速度，还将其推离不安全方向。实验结果表明，VESFlow+在4步MeanFlow模型上移除了目标概念，将Ring-A-Bell上的NudeNet攻击成功率降低至6.3%，MMA-Diffusion上降低至6.8%，同时保持了良性提示的保真度。

## Abstract
Flow matching has recently emerged as a strong paradigm for state-of-the-art text-to-image (T2I) generation, enabling high-quality generation with a small number of sampling steps. As these models are increasingly integrated into real-world applications, ensuring safe and non-sensitive content generation has become a critical requirement. However, adapting safety and concept removal methods to this new generation framework remains an open challenge. Specifically, prior methods largely rely on iterative trajectory steering across a number of denoising steps or on CLIP-centric prompt embedding manipulation. These design assumptions pose fundamental bottlenecks for safety in flow matching-based T2I generation, where limited sampling steps constrain iterative correction and modern context-aware text encoders diminish the effectiveness of embedding-level interventions. In this paper, we propose VESFlow, a training-free safety method tailored to flow matching with extremely few sampling steps. Leveraging the fact that flow matching models learn the marginal velocity, we directly edit the velocity field via a safe-conditional posterior. VESFlow steers the trajectory toward safe outputs while leaving the conditioning prompt unchanged. Building on the observation that VESFlow leaves outputs unchanged under benign prompts, we further introduce a risk score-based filtering that bypasses velocity editing to reduce computational cost while preserving benign prompt generation. Based on this filtering, we propose VESFlow+, a stronger variant of VESFlow that not only edits the velocity toward the safe direction, but also pushes it away from the unsafe direction. Experimental results show that VESFlow+ removes the target concept, reducing the attack success rate by NudeNet to 6.3% on Ring-A-Bell and 6.8% on MMA-Diffusion on the 4-step MeanFlow model, while preserving fidelity on benign prompts.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：流匹配（Flow Matching）已成为最先进的文本到图像（T2I）生成范式，能以很少的采样步数生成高质量图像。然而，随着这类模型在真实应用中广泛部署，确保生成内容的安全性（避免生成色情、暴力等有害内容）变得至关重要。现有安全方法（如基于迭代轨迹引导的负引导、基于CLIP提示嵌入编辑的方法）在流匹配的少步生成场景下面临两大瓶颈：
  - **步数限制**：少步生成（如4步）下，轨迹级校正的累积效果不足，要么安全抑制太弱，要么破坏图像保真度。
  - **文本编码器变化**：现代流匹配模型（如FLUX、SD v3）使用T5等语言模型编码器，产生句子级嵌入，使得有毒概念难以通过单个token嵌入操作精准移除。
- **整体含义**：需要为少步流匹配模型设计一种无需训练、高效的安全方法，既能有效抑制不安全概念，又能保持良性提示的生成质量和计算效率。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- **速度编辑（Velocity Editing）**：利用流匹配模型学习的是边际速度场（marginal velocity）这一特性，直接编辑速度场，使其对应于“安全条件后验”（safe-conditional posterior），即只考虑最终生成样本属于安全区域S的轨迹的平均速度。这样无需修改提示嵌入，也无需依赖多步累积校正。

### 关键技术细节

#### VESFlow
- 给定预训练条件速度场 \(v_t(x_t|c)\)，目标得到安全条件速度场 \(\tilde{v}_t(x_t|c) = v_t(x_t|c, s=1)\)，其中 \(s=1\) 表示最终样本 \(x_0\) 属于安全区域S。
- 通过贝叶斯分解和Taylor近似，推导出安全速度编辑量：
  \[
  \Delta v = \frac{t}{1 - t} \cdot \frac{\nabla_{\bar{x}} g(\bar{x})}{1 - g(\bar{x})}
  \]
  其中 \(\bar{x} = x_t - t \cdot v_t(x_t|c)\) 是预测的干净样本，\(g\) 是一个预训练的安全分类器（如NSFW检测器），输出不安全概率。该编辑项具有两个自然特性：
  - 当预测样本不安全时（\(g(\bar{x})\)接近1），编辑强度自动增大（分母变小）。
  - 时间因子 \(t/(1-t)\) 在早期采样步（\(t\)接近1）作用强，后期趋于0，符合安全引导的“关键窗口”观察。
- 为了避免 \(t \to 1\) 时发散，设置最大时间 \(t_{\text{max}} < 1\)，将因子替换为 \(t/(1 - \min(t_{\text{max}}, t))\)。
- 引入超参数 \(\lambda\) 控制引导强度，最终速度更新为 \(v \leftarrow v + \lambda \Delta v\)。

#### 扩展到MeanFlow模型
- MeanFlow学习的是两时间步间的平均速度，作者假设在近线性轨迹上速度校正变化缓慢，用端点速度编辑近似积分，因此相同的编辑公式可直接用于MeanFlow。

#### 风险分数过滤与VESFlow+
- **风险分数过滤**：基于CLIP文本嵌入计算输入提示与不安全概念集的最大余弦相似度 \(r(c)\)，当 \(r(c) > \tau\)（如0.3）时才应用VESFlow，否则跳过编辑，从而减少计算开销并保持良性提示的生成质量。
- **VESFlow+**：在过滤标识提示为不安全后，进一步将编辑从“向安全速度移动”增强为“向安全速度移动同时远离不安全速度”，即使用 \(v(x_t|c, s=1) - v(x_t|c, s=0)\) 作为编辑量，推导出更强的编辑项 \(\Delta v = \frac{t}{1 - t} \nabla_{\bar{x}} \log \frac{g(\bar{x})}{1 - g(\bar{x})}\)，同时包含吸引和排斥力。

### 算法流程（文字描述）
1. 输入提示 \(c\)，计算风险分数 \(r(c)\)；若 \(r(c) \leq \tau\)，直接使用原始速度场 \(v(x_t|c)\) 采样。
2. 若 \(r(c) > \tau\)，对于每个采样步 \(t\)（从大到小）：
   - 计算预测干净样本 \(\bar{x} = x_t - t \cdot v_t(x_t|c)\)。
   - 计算安全分类器梯度 \(\nabla_{\bar{x}} g(\bar{x})\)。
   - 计算编辑量 \(\Delta v\)（VESFlow用 \(\frac{t}{1-t} \frac{\nabla g}{1-g}\)；VESFlow+用 \(\frac{t}{1-t} \nabla \log \frac{g}{1-g}\)）。
   - 更新速度：\(v \leftarrow v + \lambda \Delta v\)，并用更新后的速度执行ODE采样步。
3. 输出最终图像 \(x_0\)。

## 3. 实验设计

### 使用的数据集与场景
- **安全评估**：
  - Ring-A-Bell（79个色情提示 + 250个暴力提示）
  - MMA-Diffusion（400个对抗性色情提示）
- **良性生成质量评估**：MS-COCO（用于计算FID和CLIP分数，使用10K或1K子集）

### 基准模型
- **FLUX.1-lite-8B**（8步采样）
- **MeanFlow蒸馏T2I模型**（4步采样，来自Pu等2025）

### 对比方法
- **无训练安全方法**：SGF（Safety-Guided Flow）、STG（Safe Text embedding Guidance）、SAFREE、Semantic Surgery（SS）
- 对这些基线进行超参数调优（引导区间、强度等），确保公平比较。

### 评价指标
- **安全指标**：攻击成功率（ASR）和有毒率（TR）
  - 色情：使用NudeNet作为评估器
  - 暴力：使用Q16评估器
- **质量指标**：FID、CLIP Score

### 实验充分性
- 主表（Table 1）报告了在两种骨干模型、两种概念（色情、暴力）上的安全与质量结果，并比较了所有基线及其与嵌入编辑方法的组合。
- 进行了多项消融实验：
  - 风险分数过滤的影响（Table 2）：无过滤时质量几乎不变，证实过滤对良性提示并非必要，但可减少计算。
  - 稳定性策略（Table 3）：比较跳过第一步、\(t_{\text{max}}=0.99\)、\(0.95\)，发现 \(t_{\text{max}}=0.99\) 安全最好。
  - 分类器鲁棒性（Figure 5）：使用LAION NSFW检测器与NudeNet作为指导分类器，并用LLaVA多模态模型独立评估，结果相似。
  - 不同采样步数的影响（Figure 7）：4步和16步。
  - 与嵌入编辑（Semantic Surgery）的协同（Table 6）。
  - 不同指导尺度下的可视化（Figure 3）。
  - 计算时间对比（Table 4）。
- 整体实验较为充分，覆盖了主要场景、多种对比方法及关键超参数影响。

## 4. 资源与算力

- **FLUX实验**：使用NVIDIA A100 GPU（40GB）。
- **MeanFlow实验**：使用4块NVIDIA GeForce RTX 4090 GPU（24GB）。
- **计算时间测量**（Table 4）：在FLUX 8步上，使用单块A100，无过滤时VESFlow每条图像约2.16秒，比基线（0.95秒）高，但远低于STG（3.61秒）；过滤后仅对不安全提示有额外开销。
- 论文未报告训练时长，因为方法无需训练。

## 5. 实验数量与充分性

- **实验组数**：主表包含2种骨干×2种概念×多种对比方法及其组合，总约20余种条件。消融实验覆盖过滤、稳定性、分类器、步数、协同、计算时间等至少6个方面。
- **充分性评价**：
  - 对比了该领域所有主流训练免费方法，并对其进行了针对少步场景的超参数调优，公平性较好。
  - 安全评估使用了两个常用数据集（Ring-A-Bell、MMA-Diffusion）和两个概念（色情、暴力）。
  - 质量评估使用MS-COCO的FID和CLIP分数，标准可靠。
  - 但论文未在更多骨干模型（如原生FLUX、SD v3）上实验，仅使用FLUX-lite和MeanFlow蒸馏模型；暴力评估数据集仅为Ring-A-Bell，规模较小（250个提示）。
  - 未实验多种不同采样步数（仅4和8步），但论文聚焦少步，可以理解。
  - 整体实验设计客观、充分，支持主要结论。

## 6. 主要结论与发现

- **VESFlow和VESFlow+在少步流匹配生成中显著提升安全性**：在4步MeanFlow上，VESFlow+将Ring-A-Bell色情提示的ASR降至6.3%，MMA-Diffusion降至6.8%，比最佳基线（SGF）有数量级提升；暴力提示ASR从0.808降至0.468。
- **良性生成质量几乎不变**：在MS-COCO上FID和CLIP分数与基线持平，过滤机制进一步确保无额外计算开销。
- **无需训练，不依赖多步累积校正**：速度编辑方式天然适合少步场景，尤其适合MeanFlow等平均速度模型。
- **鲁棒性**：使用不同安全分类器（LAION vs. NudeNet）表现一致，不依赖特定分类器。
- **计算效率**：无过滤时比基线增加约2倍时间，但过滤后对良性提示无额外开销；远低于STG。

## 7. 优点

- **方法创新**：首次针对少步流匹配模型提出速度级编辑而非轨迹级校正，与流匹配的数学特性自然结合，推导严谨且具有直觉解释。
- **实用性**：无需训练、无需修改模型参数、无需修改提示嵌入，易于部署。
- **强大的安全性能**：在极少数采样步数下显著超过现有方法，尤其VESFlow+具有双向编辑（吸引+排斥），安全性更强。
- **计算优化**：风险分数过滤设计巧妙，既保持良性提示保真度又降低计算成本。
- **实验充分**：涵盖多种骨干、多种概念、多种对比方法，消融实验验证了每个设计选择的有效性。

## 8. 不足与局限

- **超参数敏感**：引导强度 \(\lambda\) 和 \(t_{\text{max}}\) 需要调优，尤其 \(t_{\text{max}}\) 对安全性能影响显著（Table 3），可能依赖具体模型。
- **安全分类器依赖**：方法依赖一个预训练的二进制安全分类器 \(g\)，其质量直接影响效果。对于暴力等概念，训练分类器较困难（作者使用I2P数据集训练MLP），可能引入分布偏移。
- **鲁棒性验证有限**：仅测试了两种分类器（CLIP-NSFW和NudeNet），未测试更弱或对抗性攻击下的表现。
- **实验覆盖**：仅测试FLUX-lite和MeanFlow模型，未在原生FLUX、SD v3等更多流匹配模型上验证；暴力评估仅使用一个数据集（250个提示），规模较小。
- **理论上不稳定风险**：\(t/(1-t)\)因子在 \(t\) 接近1时发散，虽用 \(t_{\text{max}}\) 缓解，但可能导致早期步编辑过强，影响边界样本。
- **与嵌入编辑结合时效果退化**：Table 6显示VESFlow+与Semantic Surgery结合时性能甚至不如单独的VESFlow+，说明提示级干预与速度级干预可能存在负面交互。
- **计算开销**：无过滤时每条图像需两次前向传播（原始模型+分类器梯度），相比基线增加约100%时间，在需大规模部署时可能成为瓶颈。

（完）
