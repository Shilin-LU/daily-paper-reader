---
title: "BiWM: Advancing Open-Source Interactive Video World Models with Bidirectional Autoregression"
title_zh: "BiWM: 通过双向自回归推进开源交互式视频世界模型"
authors: "Shaohao Rui, Xiaofeng Mao, Zhanyu Zhang, Peijia Lin, Yansong Zhu, Yibo Zhang, Haibin Wan, Weijie Ma"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10135v2"
tags: ["query:vd"]
score: 9.0
evidence: 为视频世界模型提出少步蒸馏
tldr: 现有交互式视频世界模型多采用因果自回归范式，存在误差累积和质量下降问题，且训练阶段繁琐。BiWM提出首个基于双向自回归范式的全栈框架，从预训练视频骨干出发，仅需两阶段（微调控制+分布匹配蒸馏）即可得到交互式世界模型，支持多种主流模型和二次微调。通过引入GAN和前向KL目标缓解模式坍塌，并集成可插拔历史压缩与4位量化训练，实现实时相机控制和稳定长序列生成。该开源框架大幅降低了计算资源需求和训练复杂度，为资源受限的研究和高保真环境模拟提供了高效方案。
source: arxiv
selection_source: fresh_fetch
motivation: 因果自回归范式在视频世界模型中导致误差累积和质量下降，且训练阶段过多；而双向自回归方法虽效果优异，但缺乏开源实现。
method: BiWM在预训练视频骨干上先注入相机控制微调，再通过分布匹配蒸馏（DMD）将模型转化为双向自回归世界模型，结合GAN和forward-KL目标保持动态多样性。
result: 仅需两阶段训练（取代因果模型的四阶段），在8xH200 GPU上数百步收敛；支持多种模型规模，实现实时相机控制，集成历史压缩和4位量化推理。
conclusion: BiWM作为首个开源双向自回归交互式视频世界模型框架，以更低的训练成本和更高的质量推动了视频世界模型的实用化，特别适合资源受限场景。
---

## 摘要
将双向视频扩散模型转换到自回归范式可以提高视频世界模型的交互性，但现有的因果流水线需要多个阶段（控制微调、自回归训练、因果初始化、少步蒸馏），并且由于误差累积，其质量仍然落后于双向模型。最近的世界模型如Yume-1.5和Matrix-Game-3.0则采用双向自回归方法，通过自纠正误差传播获得了高保真度和稳定的长程推演，然而开源框架（如minWM）仅支持因果模型。我们提出BiWM，这是首个在双向自回归范式下用于交互式视频世界模型的全栈框架，联合优化生成质量和推理速度。基于预训练视频骨干网络，BiWM通过微调注入摄像机控制，然后运行一个少步分布匹配蒸馏（DMD）阶段，将骨干网络转变为动作/摄像机可控的世界模型：仅需两个训练阶段（而minWM需要四个），在8块H200 GPU上仅数百步即可收敛。单一方案涵盖了Wan2.1-1.3B、Wan2.2-5B、HunyuanVideo-1.5-8B和LTX-2.3-22B，并支持对现有双向模型进行二次微调。BiWM实现了minWM失控的真实世界摄像机控制，集成了可插拔的历史压缩（FramePack风格和PackForcing风格）以支持长程推演，并提供可选的NVFP4 4位训练/推理流水线。为应对DMD的模式寻求退化，我们加入生成对抗网络和覆盖质量的前向KL散度目标以保持场景动态。我们将BiWM开源，用于资源受限的研究和高保真环境模拟。

## Abstract
Transitioning bidirectional video diffusion models into an autoregressive paradigm improves the interactivity of video world models, but existing causal pipelines need many stages (control fine-tuning, autoregressive training, causal initialization, few-step distillation) and still trail bidirectional models in quality due to error accumulation. Recent world models such as Yume-1.5 and Matrix-Game-3.0 instead adopt a bidirectional autoregressive approach, gaining fidelity and stable long-horizon rollout from self-correcting error propagation, yet open-source frameworks (e.g., minWM) support only causal models. We present BiWM, the first full-stack framework for interactive video world models under the bidirectional autoregressive paradigm, jointly optimizing generation quality and inference speed. From a pretrained video backbone, BiWM injects camera control by fine-tuning, then runs a few-step Distribution Matching Distillation (DMD) stage that turns the backbone into an action/camera-controllable world model: just two training stages instead of four in minWM, converging in a few hundred steps on 8xH200 GPUs. A single recipe spans Wan2.1-1.3B, Wan2.2-5B, HunyuanVideo-1.5-8B, and LTX-2.3-22B, and also supports secondary fine-tuning of existing bidirectional models. BiWM enables real-world camera control where minWM loses controllability, integrates pluggable history compression (FramePack-style and PackForcing-style) for long rollouts, and offers an optional NVFP4 4-bit training/inference pipeline. To counter DMD's mode-seeking degradation, we add GAN and mass-covering forward-KL objectives that preserve scene dynamics. We open-source BiWM for resource-constrained research and high-fidelity environment simulation.

---

## 论文详细总结（自动生成）

# BiWM: 通过双向自回归推进开源交互式视频世界模型 — 详细总结

## 1. 核心问题与整体含义

- **研究动机**：现有交互式视频世界模型大多采用**因果自回归**范式，生成时依赖先前帧作为条件，导致误差累积和长序列质量下降。同时，因果模型的训练流程繁琐，通常需要四个阶段（控制微调、自回归训练、因果初始化、少步蒸馏），计算成本高且效果仍落后于双向模型。
- **研究背景**：近期工作（如Yume-1.5、Matrix-Game-3.0）采用**双向自回归**方法，通过自纠正误差传播实现了高保真度和稳定的长程推演，但缺乏开源实现。现有开源框架（如minWM）仅支持因果模型。
- **整体意义**：BiWM作为首个开源的双向自回归交互式视频世界模型全栈框架，仅需两阶段训练即可获得高质量的可控视频世界模型，大幅降低训练复杂度和资源需求，推动视频世界模型在资源受限场景下的实用化。

## 2. 方法论

### 核心思想
- 从预训练视频骨干网络出发，通过两阶段流程将模型转化为**双向自回归**的交互式世界模型，同时联合优化生成质量和推理速度。

### 关键技术细节
1. **第一阶段：控制微调**  
   - 在预训练骨干网络上注入**相机控制**（如动作/视角参数），通过微调使模型具备可控性。
2. **第二阶段：分布匹配蒸馏**  
   - 运行**少步分布匹配蒸馏**，将微调后的骨干网络转化为动作/相机可控的双向自回归世界模型。  
   - 仅需数百步即可收敛。
3. **对抗模式坍塌**  
   - 引入**生成对抗网络**和**覆盖质量的前向KL散度**目标，保留场景动态多样性。
4. **历史压缩与量化**  
   - 集成可插拔历史压缩模块（FramePack风格和PackForcing风格），支持长序列推演。  
   - 提供可选的**NVFP4 4位训练/推理流水线**，降低内存和计算开销。
5. **模型兼容性**  
   - 单个方案覆盖多种规模骨干：Wan2.1-1.3B、Wan2.2-5B、HunyuanVideo-1.5-8B、LTX-2.3-22B。  
   - 同时支持对现有双向模型进行二次微调。

### 算法流程（文字说明）
- 输入：预训练视频骨干 → 控制微调（加入相机参数） → DMD蒸馏（配合GAN+forward-KL） → 输出：双向自回归可控世界模型。  
- 推理时：利用历史压缩和4位量化实现实时长序列生成。

## 3. 实验设计

- **数据集/场景**：论文未明确列出具体基准数据集，但提到在**真实世界摄像机控制**场景下测试，与minWM对比显示BiWM保持了可控性而minWM失控。  
- **基准方法**：主要对比**minWM**（因果自回归框架）以及其他因果流水线。  
- **对比方法**：未详细列举更多方法，但暗示与Yume-1.5、Matrix-Game-3.0等双向方法对标（因后者非开源，无法直接对比）。  
- **评估维度**：包括摄像机控制精度、长序列稳定性、生成质量（主观/定性描述），但文中缺乏定量指标（如FVD、PSNR等）。

## 4. 资源与算力

- **硬件**：使用 **8×H200 GPU**（NVIDIA H200，80GB显存）。  
- **训练时长**：整个两阶段流程仅需 **数百步收敛**（未明确给出具体步数或小时数）。  
- **其他**：支持NVFP4 4位量化训练/推理，进一步降低资源需求。

## 5. 实验数量与充分性

- **实验数量**：文中主要呈现了**定性结果**和跨模型规模（1.3B～22B）的适用性验证，以及历史压缩、量化等消融模块（但消融细节不够详细）。  
- **充分性评估**：  
  - **优点**：覆盖了多个骨干模型、不同控制类型、历史压缩与量化选项，验证了框架的通用性。  
  - **不足**：缺少**定量对比表格**（如与minWM在相同指标下的数值差异），未在标准视频预测基准（如Kinetics-600、UCF101）上报告结果，实验客观性有限，说服力不足。  
  - **公平性**：未与因果模型的同类蒸馏方法（如minWM的四个阶段）进行同设置下的量化对比。

## 6. 主要结论与发现

- BiWM作为**首个开源双向自回归交互式视频世界模型框架**，将训练阶段从因果模型的四阶段减少为两阶段，且在8×H200 GPU上仅需数百步即可收敛。  
- 相比因果模型（minWM），BiWM实现了**有效的真实世界摄像机控制**（minWM在该场景下失控），并支持稳定长序列生成。  
- 通过引入GAN和forward-KL目标，有效缓解了DMD蒸馏中的模式坍塌问题，保持了场景动态多样性。  
- 可插拔历史压缩和4位量化使得长推演和资源有限部署成为可能。  
- 开源后将极大促进资源受限研究社区在高保真环境模拟上的探索。

## 7. 优点

- **训练高效**：仅两阶段，大幅降低计算和调参成本。  
- **全栈开源**：覆盖训练、推理、压缩、量化，提供完整工具箱。  
- **模型广度**：支持4种主流骨干（1.3B～22B），且可二次微调。  
- **生成质量**：双向自回归机制自纠正误差，长序列保真度高。  
- **技术组合创新**：DMD + GAN + forward-KL的蒸馏策略、可插拔历史压缩、NVFP4量化等，兼顾质量与速度。  
- **针对性解决局限**：明确针对因果模型误差累积和训练阶段繁琐的痛点，提供实用解决方案。

## 8. 不足与局限

- **实验定量不足**：未提供客观的数值指标（如FVD、PSNR、LPIPS、推理FPS等），仅依赖定性描述，削弱了可复现性和可信度。  
- **缺乏标准 benchmark 验证**：未在广泛使用的视频预测/世界模型数据集上评测，难以与现有方法直接对比。  
- **控制类型单一**：主要聚焦相机控制，未涉及更复杂的动作/交互控制（如物体操控、多智能体）。  
- **模式坍塌缓解评估不充分**：虽有GAN+forward-KL设计，但缺乏消融实验展示其具体贡献量。  
- **量化精度风险**：4位量化可能引入精度损失，文中未分析对生成质量的具体影响。  
- **泛化性未知**：未评估在未见过的环境或动作分布下的鲁棒性。  
- **计算资源虽低但仍受限**：8×H200 GPU对于个人/小团队仍较高，且未给出更小预算的可行方案（如单卡微调）。

（完）
