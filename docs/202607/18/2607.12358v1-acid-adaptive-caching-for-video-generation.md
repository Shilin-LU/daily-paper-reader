---
title: "ACID: Adaptive Caching for vIDeo generation"
title_zh: ACID：面向视频生成的适应性缓存方法
authors: "Om Agrawal, Saurabh Agarwal, Aditya Akella"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12358v1"
tags: ["query:fast-gen"]
score: 9.0
evidence: 自适应缓存加速视频生成且不损失质量
tldr: "视频扩散模型推理缓慢，现有缓存加速方法因固定阈值而面临质量与速度的权衡。本文发现存在关键时间步，其漂移信号变化剧烈，需低阈值处理。提出ACID，一种轻量级无训练包装器，通过监控漂移信号变化率动态切换高低阈值，在多个模型和方法上扩展了质量-速度帕累托前沿。在HunyuanVideo上实现2.16倍加速，相比保守固定阈值额外提速38%，质量损失可忽略。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12358v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 551}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1438, \"height\": 548}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1438, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1454, \"height\": 1394}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1450, \"height\": 924}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1453, \"height\": 961}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12358v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 1315}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1217, \"height\": 382}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1229, \"height\": 381}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1229, \"height\": 418}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1287, \"height\": 918}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1268, \"height\": 814}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1234, \"height\": 813}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1249, \"height\": 668}]"
motivation: 现有动态缓存方法使用固定阈值，导致质量与速度的次优权衡；观察发现存在关键时间步，其漂移信号变化剧烈，需差异化处理。
method: ACID通过实时监控现有漂移信号的变化率，动态在低阈值与高阈值之间切换，以适配不同时间步的敏感性，无需重新训练。
result: "在TeaCache、EasyCache、DiCache三种方法及HunyuanVideo、Wan 2.1、CogVideoX三个模型上，ACID均扩展了帕累托前沿，最高加速2.16倍，额外提速38%，质量损失低于0.3 dB PSNR。"
conclusion: 自适应阈值策略有效突破了固定阈值的局限，为缓存加速方法提供了一种通用、即插即用的改进方案，显著提升推理效率。
---

## 摘要
视频扩散模型能生成高质量内容，但由于其顺序去噪过程，推理速度较慢。基于缓存的加速方法通过复用中间模型输出来解决这一问题：诸如TeaCache、EasyCache和DiCache等领先的动态方法会累积一个漂移信号，当累积漂移低于固定阈值τ时，跳过昂贵的模型评估。该阈值控制着一个明显的权衡——提高阈值可以加快生成速度但牺牲视觉质量，降低阈值则保留质量但牺牲速度。我们表明这一权衡并非本质性；它是去噪过程中保持τ恒定的产物。我们识别出关键步骤的存在——即漂移信号快速变化的时间步——并表明在这些步骤上选择性应用低阈值，同时在别处积极缓存，能以显著更高的推理速度恢复保守缓存的大部分质量。基于这一洞察，我们提出了ACID，一种轻量级、无需训练的外挂方法，它监控每种方法现有漂移信号的变化率，以动态切换低阈值和高阈值。ACID是信号无关和模块化的：它无需重新训练，可直接插入现有的动态缓存方法，无需修改其核心机制。在三种缓存方法（TeaCache、EasyCache、DiCache）和三个开源视频扩散模型（HunyuanVideo、Wan 2.1、CogVideoX）上的评估中，ACID始终将视觉质量与推理速度的帕累托前沿扩展到任何固定阈值所能达到的范围之外。特别是，在TeaCache和HunyuanVideo上，ACID相对于无缓存基线实现了高达2.16倍的加速，相对于保守的固定阈值基线实现了高达38%的额外加速，且质量退化可忽略不计（<0.3 dB PSNR、<0.01 SSIM、<0.01 LPIPS）。

## Abstract
Video diffusion models produce high-quality generations but remain slow at inference due to their sequential denoising procedure. Caching-based acceleration methods address this by reusing intermediate model outputs: leading dynamic approaches such as TeaCache, EasyCache, and DiCache accumulate a drift signal and skip expensive model evaluations when accumulated drift stays below a fixed threshold τ. This threshold controls an apparent tradeoff - raising it yields faster generation at the cost of visual quality, while lowering it preserves quality but sacrifices speed. We show this tradeoff is not fundamental; it is an artifact of holding τ constant throughout denoising. We identify the existence of critical steps - timesteps where the drift signal changes rapidly - and show that applying a low threshold selectively at these steps while caching aggressively elsewhere recovers most of the quality of conservative caching at substantially higher inference speeds. Building on this insight, we propose ACID, a lightweight, training-free wrapper that monitors the rate of change of each method's existing drift signal to dynamically switch between a low and a high threshold. ACID is signal-agnostic and modular: it requires no retraining and plugs directly into existing dynamic caching methods without modifying their core mechanisms. Evaluated across three caching methods (TeaCache, EasyCache, DiCache) and three open-source video diffusion models (HunyuanVideo, Wan 2.1, CogVideoX), ACID consistently expands the Pareto frontier of visual quality versus inference speed beyond what any fixed threshold achieves. In particular, on TeaCache and HunyuanVideo, ACID achieves up to 2.16x speedup over the no-caching baseline, and up to 38% additional speedup over the conservative fixed-threshold baseline with negligible (<0.3 dB PSNR, <0.01 SSIM, <0.01 LPIPS) quality degradation.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：视频扩散模型（如 HunyuanVideo、Wan 2.1、CogVideoX）生成高质量视频，但推理速度极慢（在单张 A100 上生成约 5 秒视频需 3–23 分钟），主要瓶颈在于顺序去噪过程中反复执行昂贵的扩散主干。
- **现有方法**：基于缓存的加速方法（如 TeaCache、EasyCache、DiCache）通过累加一个“漂移信号”并对比固定阈值 τ 来决定是否跳过模型计算，从而提速。
- **核心矛盾**：固定阈值导致质量与速度之间的“两难权衡”——高阈值加速但质量下降，低阈值保质量但速度慢。论文认为这种权衡并非本质，而是固定阈值策略的产物。

### 2. 论文提出的方法论

#### 核心思想
- **关键步骤的发现**：在去噪过程中存在“关键步骤”（critical steps），此时漂移信号变化剧烈（第二导数大），对最终质量影响重大；而在稳定阶段漂移变化缓慢，可以积极缓存。
- **自适应阈值**：放弃单一的固定阈值，改为在关键步骤使用低阈值（保守缓存），在非关键步骤使用高阈值（激进缓存），从而在几乎不损失质量的情况下大幅提速。

#### 关键技术细节
- **信号监控**：利用现有动态缓存方法本身的漂移信号（如 TeaCache 的 ΔTEMNI、EasyCache 的 k×Δinput/∥vₜ₋₁∥、DiCache 的浅层特征变化），计算 **二阶导数（变化率）** 来判断当前步骤属于“动态阶段”还是“稳定阶段”。
- **双阈值策略**：
  - 维护两个阈值：`τlow`（低阈值，用于关键步骤）和 `τhigh`（高阈值，用于稳定步骤）。
  - 若当前信号变化率在区间 `[−δ, δ]` 内，视为稳定，使用 `τhigh`；否则视为动态，使用 `τlow`。
  - 该决策在每个步骤动态进行，无需重新训练。
- **算法流程**（Algorithm 1）：
  1. 初始化累积漂移 Σ = 0，前一信号 `sprev = None`。
  2. 对于每个去噪步骤 t（从 T 到 0）：
     - 若处于预热阶段，执行完整模型并更新。
     - 否则，计算当前信号 `sₜ`，累加至 Σ。
     - 根据 `|sₜ - sprev| ≤ δ` 判断稳定/动态，选择 τ。
     - 若 Σ ≤ τ，跳过模型，用缓存重构；否则执行完整模型，重置 Σ。
  3. 更新 `sprev = sₜ`。

#### 公式或算法说明
- 无复杂公式，核心为阈值选择逻辑：
  ```plaintext
  if |sₜ - sprev| ≤ δ:
      τ ← τhigh
  else:
      τ ← τlow
  ```
- 信号 sₜ 直接复用原方法（TeaCache/EasyCache/DiCache）已有指标，无需额外计算。

### 3. 实验设计

- **数据集/场景**：使用 VBench 的 33 个提示词（prompt），从 11 个维度组中每类选 3 个 prompt，覆盖全部 16 个 VBench 评估维度。
- **Benchmark**：三个开源视频扩散模型：
  - HunyuanVideo T2V 720P
  - Wan 2.1 T2V 1.3B
  - CogVideoX 1.5-5B
- **对比方法**：
  - 三种动态缓存方法：TeaCache、EasyCache、DiCache。
  - 每个方法下对比：无缓存基线（baseline）、多个固定阈值配置（low/mid/high）、以及 ACID 自适应双阈值配置。
  - EasyCache 和 DiCache 在 CogVideoX 上无官方实现，因此未评估。
- **评估指标**：推理延迟（latency）、加速比（speedup）、视觉质量（PSNR↑、SSIM↑、LPIPS↓），均以无缓存基线为参考。
- **总组合数**：7 个 method×model 组合（TeaCache×3 模型、EasyCache×2、DiCache×2），每个组合测试了多个固定阈值和多个自适应配置（详见附录 B 表格），实验总量约 50+ 组配置。

### 4. 资源与算力

- **GPU 型号**：NVIDIA A100 80GB PCIe。
- **数量**：文中未明确说明具体数量，但根据常见实验设置推测使用单卡或少量卡（A100 80G 可容纳上述模型）。
- **训练时长**：ACID 是 **training-free** 方法，无需额外训练。实验仅涉及推理评估，推理耗时已在评价指标中体现（典型 102–1359 秒/视频）。
- **其他资源**：PyTorch 框架，使用固定种子确保可重复性。

### 5. 实验数量与充分性

- **实验数量**：附录 B 列出了全部 50+ 个配置的详细结果，覆盖每个 method×model 下的至少 5–10 个阈值的固定点，以及 3–8 个自适应双阈值点。例如 TeaCache×HunyuanVideo 有 8 个点，EasyCache×HunyuanVideo 有 24 个点。
- **充分性**：
  - **模型多样性**：涵盖 3 个主流开源模型（含不同架构），验证了通用性。
  - **方法多样性**：覆盖三种不同信号设计的缓存方法（输入代理、变换率、特征探测）。
  - **质量评估**：使用 PSNR、SSIM、LPIPS 三种客观指标，且均与无缓存基线对比——公平。
  - **消融/分析**：展示了二阶导数信号与最优切换点的一致性（图 4、5），通过固定阈值与自适应阈值的 Pareto 前沿对比验证优势。
  - **客观性**：使用固定种子、统一 prompt 集、官方实现，降低偏差。
- **不足**：未进行人工主观评测；缺乏与其他加速范式的直接对比（如蒸馏、稀疏注意力）；未测试更大模型或更长视频。

### 6. 论文的主要结论与发现

1. **固定阈值非最优**：去噪过程中存在关键步骤，使用单一阈值必然导致次优质量-速度权衡。
2. **ACID 有效扩展 Pareto 前沿**：在所有 7 个 model×method 组合上，ACID 的自适应配置均能取得比任何固定阈值配置更好的质量-速度权衡。
3. **显著加速且质量可忽略**：在 TeaCache×HunyuanVideo 上，ACID 达 2.16× 加速，比保守固定阈值额外提速 38%，PSNR 下降仅 <0.3 dB，SSIM 下降 <0.01，LPIPS 上升 <0.01。
4. **通用易用**：ACID 是方法无关、信号无关的无训练外挂模块，可直接插入现有动态缓存方法。

### 7. 优点

- **洞见清晰**：识别关键步骤，用二阶导数作为切换信号，直觉强且简洁。
- **即插即用**：不修改原方法核心，仅在外层调整阈值，兼容性强。
- **无训练开销**：完全无需训练，适合部署。
- **验证充分**：跨越三种缓存方法、三种模型、多种阈值配置，实验设计系统、全面。
- **量化分析**：给出了 Pareto 前沿展示，视觉效果直观。

### 8. 不足与局限

- **缺乏主观评价**：仅使用客观指标（PSNR/SSIM/LPIPS），未进行人类感知评测，尤其在高加速比下质量损失可能被指标低估。
- **未对比其他加速方法**：如蒸馏（LCM）、稀疏注意力（Sparse VideoGen）、量化等横向比较缺少，无法体现相对优势。
- **阈值选择仍需人工**：`τlow`、`τhigh`、`δ` 三个超参数需根据模型/方法调优，文中未提供自动调优方案。
- **覆盖模型有限**：未测试更大参数量的模型（如 Wan 2.1 14B）或更复杂视频生成任务。
- **延迟评估环境控制**：未提及 GPU 个数、批处理大小，可能影响速度数据绝对性（但相对加速比有效）。
- **信号稳定性依赖**：ACID 依赖于原方法信号的可信度，若原信号本身不可靠（如 DiCache 在某些模型表现差），ACID 也会受限。

（完）
