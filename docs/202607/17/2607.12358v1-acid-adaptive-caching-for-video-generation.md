---
title: "ACID: Adaptive Caching for vIDeo generation"
title_zh: "ACID: 面向视频生成的自适应缓存"
authors: "Om Agrawal, Saurabh Agarwal, Aditya Akella"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12358v1"
tags: ["query:fast-gen"]
score: 8.0
evidence: 自适应缓存加速视频生成
tldr: 视频扩散模型推理慢，现有动态缓存方法固定阈值导致质量-速度权衡不佳。本文发现关键步存在剧烈漂移，提出ACID根据漂移变化率动态切换阈值。ACID作为轻量无训练包装器，即插即用于TeaCache等方法，在HunyuanVideo等模型上扩展了Pareto前沿，最高实现2.16倍加速且质量损失可忽略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12358v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 368, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1438, \"height\": 548, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1438, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1454, \"height\": 1394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1450, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1453, \"height\": 961, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12358v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 1315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1217, \"height\": 382, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1229, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1229, \"height\": 418, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1287, \"height\": 918, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1268, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1234, \"height\": 813, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1249, \"height\": 668, \"label\": \"Table\"}]"
motivation: 现有动态缓存方法固定阈值导致关键步质量下降或非关键步速度受限，质量-速度权衡非最优。
method: ACID监测漂移信号变化率，动态在关键步使用低阈值、其他步使用高阈值，无需重训。
result: "在TeaCache等方法上，ACID相比保守基线额外加速38%，质量损失低于0.3 dB PSNR。"
conclusion: ACID以轻量无训练方式扩展了动态缓存方法的Pareto前沿，实现更优质量-速度权衡。
---

## 摘要
视频扩散模型能够产生高质量的生成结果，但由于其顺序去噪过程，推理速度仍然较慢。基于缓存的加速方法通过重复使用中间模型输出来解决这一问题：主流的动态方法如TeaCache、EasyCache和DiCache会积累漂移信号，并在累积漂移保持在固定阈值τ以下时跳过昂贵的模型评估。这个阈值控制着一个明显的权衡——提高阈值会以牺牲视觉质量为代价实现更快的生成，而降低阈值则保持质量但牺牲速度。我们表明这种权衡并非本质性的；它是整个去噪过程中保持τ恒定的人为产物。我们识别出关键步骤的存在——即漂移信号快速变化的时间步——并表明在这些步骤中根据应用低阈值，而在其他地方积极缓存，可以在显著更高的推理速度下恢复保守缓存的大部分质量。基于这一见解，我们提出了ACID，一种轻量级、无需训练的包装器，它监控每种方法现有漂移信号的变化率，以动态地在低阈值和高阈值之间切换。ACID是信号无关且模块化的：它不需要重新训练，可直接插入现有的动态缓存方法中，而无需修改其核心机制。在三种缓存方法（TeaCache、EasyCache、DiCache）和三个开源视频扩散模型（HunyuanVideo、Wan 2.1、CogVideoX）上的评估表明，ACID持续扩展了视觉质量与推理速度之间的帕累托前沿，超越了任何固定阈值所能达到的效果。特别是，在TeaCache和HunyuanVideo上，ACID相比无缓存基线实现了高达2.16倍的加速，相比保守的固定阈值基线实现了高达38%的额外加速，且质量下降可忽略不计（<0.3 dB PSNR, <0.01 SSIM, <0.01 LPIPS）。

## Abstract
Video diffusion models produce high-quality generations but remain slow at inference due to their sequential denoising procedure. Caching-based acceleration methods address this by reusing intermediate model outputs: leading dynamic approaches such as TeaCache, EasyCache, and DiCache accumulate a drift signal and skip expensive model evaluations when accumulated drift stays below a fixed threshold τ. This threshold controls an apparent tradeoff - raising it yields faster generation at the cost of visual quality, while lowering it preserves quality but sacrifices speed. We show this tradeoff is not fundamental; it is an artifact of holding τ constant throughout denoising. We identify the existence of critical steps - timesteps where the drift signal changes rapidly - and show that applying a low threshold selectively at these steps while caching aggressively elsewhere recovers most of the quality of conservative caching at substantially higher inference speeds. Building on this insight, we propose ACID, a lightweight, training-free wrapper that monitors the rate of change of each method's existing drift signal to dynamically switch between a low and a high threshold. ACID is signal-agnostic and modular: it requires no retraining and plugs directly into existing dynamic caching methods without modifying their core mechanisms. Evaluated across three caching methods (TeaCache, EasyCache, DiCache) and three open-source video diffusion models (HunyuanVideo, Wan 2.1, CogVideoX), ACID consistently expands the Pareto frontier of visual quality versus inference speed beyond what any fixed threshold achieves. In particular, on TeaCache and HunyuanVideo, ACID achieves up to 2.16x speedup over the no-caching baseline, and up to 38% additional speedup over the conservative fixed-threshold baseline with negligible (<0.3 dB PSNR, <0.01 SSIM, <0.01 LPIPS) quality degradation.

---

## 论文详细总结（自动生成）

# ACID: Adaptive Caching for vIDeo generation - 详细中文总结

## 1. 论文的核心问题与整体含义
- **研究动机**：视频扩散模型（如HunyuanVideo、Wan 2.1、CogVideoX）生成高质量视频但推理速度极慢（单个A100上约3-23分钟/5秒视频），主要瓶颈在于顺序去噪过程。现有动态缓存加速方法（TeaCache、EasyCache、DiCache）通过累积漂移信号并与**固定阈值**比较来决定是否跳过模型计算，这导致了视觉质量与推理速度之间的明显权衡。
- **核心问题**：这种权衡是否是本质性的？论文发现**并非本质**，而是固定阈值策略的人为产物。去噪过程中存在“关键步骤”（漂移信号快速变化的时间步），在这些步上使用低阈值保留质量，而在其他步上使用高阈值积极缓存，可以显著改善权衡。
- **整体含义**：提出了一种轻量、无训练的包装器ACID，通过动态监测漂移信号的变化率，自适应地切换低/高阈值，从而在不牺牲质量的前提下大幅提升推理速度，扩展了现有方法的帕累托前沿。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
### 核心思想
- 识别去噪过程中的“关键步骤”（critical steps），这些步骤的漂移信号变化率大（二阶导数波动大），对质量影响大；其余步骤信号稳定。通过动态调整缓存阈值——在关键步用低阈值（保守缓存），在稳定步用高阈值（积极缓存）——可以突破固定阈值带来的质量-速度次优性。

### 关键技术细节
- **信号选择**：ACID是信号无关的，可应用于任何动态缓存方法的已有漂移信号（如TeaCache的∆TEMNI、EasyCache的k×∆input/∥vt−1∥、DiCache的∆probe features）。
- **阈值切换依据**：使用信号的**一阶差分**（即变化率）来判断稳定性。若当前步信号变化率`|s_t - s_{t-1}|`小于等于稳定性边界δ，则认为处于稳定阶段，使用高阈值τ_high（积极缓存）；否则使用低阈值τ_low（保守缓存）。
- **实现**：作为一种包装器（wrapper），不修改原方法的核心机制（漂移估计、输出重建），仅替换固定阈值为自适应双阈值。

### 算法流程（伪代码描述）
1. 初始化：累积漂移Σ=0，上一个信号s_prev=None。
2. 对每个去噪步t（从T到0）：
   - 如果是预热阶段（warmup），运行完整模型并更新方法状态。
   - 否则：
     - 计算当前漂移信号s_t（通过原方法M）。
     - 将s_t累加到Σ。
     - 判断稳定性：`if s_prev is not None and |s_t - s_prev| ≤ δ`，则选用τ_high；否则选用τ_low。
     - 如果Σ ≤ 当前选中的τ，则跳过完整模型，使用原方法的复用机制（M.reuse）重建输出。
     - 否则，运行完整模型f_θ，更新方法状态，重置Σ=0。
   - 更新s_prev = s_t。

## 3. 实验设计：数据集、benchmark、对比方法
- **数据集/场景**：从VBench的16个评估维度中选取33个提示词（每个维度对应若干组，每组3个提示），覆盖所有16个维度。对每个方法-模型组合，使用固定种子生成33个视频（每个提示一个视频）进行评估。
- **Benchmark**：以无缓存（no-caching）基线为参考计算PSNR、SSIM、LPIPS；报告推理延迟（秒）和加速比（无缓存延迟/方法延迟）。
- **对比方法**：
  - 三种基础动态缓存方法：TeaCache、EasyCache、DiCache（仅在官方支持的模型上测试）。
  - 三种视频扩散模型：HunyuanVideo T2V 720P、Wan 2.1 T2V 1.3B、CogVideoX 1.5-5B。
  - 对比配置：无缓存基线、固定阈值低值、固定阈值高值、以及ACID自适应双阈值变体（多个不同τ_low/τ_high/δ组合）。
- **总实验组合**：7个方法-模型对（TeaCache×3模型+EasyCache×2模型+DiCache×2模型），每个对比多个阈值设置（见附录B表格，总共约60+行配置）。

## 4. 资源与算力
- **GPU型号**：所有实验在NVIDIA A100 80GB PCIe GPU上进行。
- **数量与时长**：论文**未明确说明**使用的GPU数量、每个实验的运行时长或总计算量。仅提到生成33个视频（每个种子固定）用于每种配置，且方法本身是训练-free的，因此主要消耗是推理时间。考虑到模型规模和视频生成时间，总计算量可观但符合该领域标准。论文未提供训练阶段算力（因为是训练-free）。

## 5. 实验数量与充分性
- **实验数量**：涵盖3种模型、3种缓存方法、多种阈值设置（固定阈值多个值、自适应多个组合），在33个提示上生成视频，共计约7×10+组配置，每组33个视频，实验量较为充分。
- **充分性与公平性**：
  - 论文提供了详细的Pareto图（附录A）和完整配置表（附录B），展示了所有测试点的质量-延迟权衡，透明且可验证。
  - 对比了每种方法固定阈值下的最佳点与ACID的自适应点，公平。
  - 但**不足**：未对EasyCache和DiCache在CogVideoX上进行评估（因缺乏官方实现），这限制了跨模型泛化性的验证。此外，未进行消融实验单独验证阈值切换准则（如用其他规则替代二阶导数）的贡献，仅通过小实验展示了最优切换点与二阶导数稳定的对齐。

## 6. 论文的主要结论与发现
- 固定阈值动态缓存方法的质量-速度权衡是人为的，通过自适应阈值可以显著改善。
- 去噪过程中存在“关键步骤”，其漂移信号变化率大，在这些步骤使用低阈值缓存可以保护质量，而在稳定步骤使用高阈值缓存可以获得速度。
- ACID作为轻量、无训练、模块化的包装器，能够将任何基于阈值的动态缓存方法的帕累托前沿向外扩展，在保持质量几乎不变（<0.3 dB PSNR, <0.01 SSIM, <0.01 LPIPS）的同时，获得高达38%的额外加速（相对于保守固定阈值基线），最高2.16倍加速于无缓存基线。
- 结论对不同模型（U型信号模式如HunyuanVideo/Wan 2.1，以及不同分布如CogVideoX）和不同缓存信号均成立，表明方法的通用性。

## 7. 优点
- **方法简洁高效**：ACID仅通过监测现有漂移信号的变化率来切换阈值，无需额外训练或修改原有缓存机制，计算开销极低。
- **通用性**：信号无关，可即插即用于TeaCache、EasyCache、DiCache等多种动态缓存方法。
- **实验结果透明**：提供了完整的Pareto图和详细数值表格，支持复现和比较。
- **识别关键结构**：揭示了扩散去噪过程中的“关键步骤”现象，并提供了简单有效的应对策略，具有理论洞察价值。
- **实际效果显著**：在多个模型上一致提升了帕累托前沿，兼顾速度与质量。

## 8. 不足与局限
- **实验覆盖不全**：未在CogVideoX上测试EasyCache和DiCache（缺乏官方实现），跨模型泛化性验证不够充分。
- **消融实验缺失**：没有单独分析不同稳定性边界δ的影响，也没有与更复杂的自适应策略（如连续阈值变化）对比，仅比较了两种固定阈值和双阈值。
- **评估指标局限**：仅使用PSNR、SSIM、LPIPS与无缓存基线对比，未进行人类主观评价或下游任务评估，可能存在感知质量与指标不一致的风险。
- **应用限制**：ACID依赖于原方法提供的漂移信号，若信号本身不准确或噪声大，可能导致误判；此外，阈值τ_low和τ_high以及δ需要手动设定（实验中通过网格搜索），未提供自动调优方法。
- **计算资源描述不足**：未报告总计算量、GPU数量、运行时间等，不利于资源公平比较和复现成本评估。
- **对其他架构的适用性未知**：仅测试了Transformer-based扩散模型，未涉及U-Net等架构。

（完）
