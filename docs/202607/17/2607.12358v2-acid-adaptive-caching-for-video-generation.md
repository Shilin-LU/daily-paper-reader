---
title: "ACID: Adaptive Caching for vIDeo generation"
title_zh: ACID：面向视频生成的自适应缓存
authors: "Om Agrawal, Saurabh Agarwal, Aditya Akella"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12358v2"
tags: ["query:fast-gen"]
score: 8.0
evidence: 自适应缓存加速视频扩散模型
tldr: "视频扩散模型因顺序去噪过程推理缓慢。现有缓存加速方法采用固定阈值控制中间输出重用，导致速度与质量权衡。ACID通过监测漂移信号变化率，动态在关键步骤切换低阈值、其他步骤高阈值，打破此权衡。在三种缓存方法和三个模型上，ACID扩展了帕累托前沿，如TeaCache+HunyuanVideo实现2.16x加速，比固定阈值额外快38%，质量损失可忽略。该方法轻量、无训练、即插即用。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12358v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 368, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1438, \"height\": 548, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1438, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1454, \"height\": 1394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1450, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1453, \"height\": 961, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12358v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 1315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1217, \"height\": 382, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1229, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1229, \"height\": 418, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1287, \"height\": 918, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1268, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1234, \"height\": 813, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1249, \"height\": 668, \"label\": \"Table\"}]"
motivation: 固定阈值缓存方法存在速度与质量的刚性权衡，而临界步骤的存在表明可通过自适应阈值来打破这一权衡。
method: ACID作为轻量无训练包装器，基于现有漂移信号的变化率动态切换低和高缓存阈值。
result: "在TeaCache+HunyuanVideo上，ACID实现2.16x加速，比保守固定阈值额外提速38%，PSNR下降<0.3 dB。"
conclusion: ACID模块化且信号无关，可扩展帕累托前沿，大幅提升缓存加速的效率与质量。
---

## 摘要
视频扩散模型能够生成高质量内容，但由于其顺序去噪过程，推理速度较慢。基于缓存的加速方法通过重用中间模型输出来解决这一问题：主流的动态方法（如TeaCache、EasyCache和DiCache）会累积一个漂移信号，并在累积漂移低于固定阈值τ时跳过昂贵的模型评估。该阈值控制着一个明显的权衡——提高阈值可加快生成速度，但会牺牲视觉质量；降低阈值则保持质量但牺牲速度。我们证明这种权衡并非本质性的，而是由于在整个去噪过程中保持τ恒定所致。我们识别出关键步骤的存在——即漂移信号快速变化的时间步——并表明在这些步骤上选择性地应用低阈值，同时在其他地方积极缓存，可以在显著更高的推理速度下恢复保守缓存的大部分质量。基于这一洞察，我们提出ACID，一种轻量级、无需训练的外挂模块，通过监控各方法现有漂移信号的变化率，动态地在低阈值和高阈值之间切换。ACID与信号无关且模块化：它无需重新训练，可直接插入现有的动态缓存方法，而不修改其核心机制。在三种缓存方法（TeaCache、EasyCache、DiCache）和三个开源视频扩散模型（HunyuanVideo、Wan 2.1、CogVideoX）上的评估表明，ACID始终将视觉质量与推理速度的帕累托前沿扩展到任何固定阈值所能达到的范围之外。特别地，在TeaCache和HunyuanVideo上，ACID相比无缓存基线实现了高达2.16倍的加速，相比保守的固定阈值基线实现了高达38%的额外加速，而质量下降可忽略不计（PSNR <0.3 dB，SSIM <0.01，LPIPS <0.01）。

## Abstract
Video diffusion models produce high-quality generations but remain slow at inference due to their sequential denoising procedure. Caching-based acceleration methods address this by reusing intermediate model outputs: leading dynamic approaches such as TeaCache, EasyCache, and DiCache accumulate a drift signal and skip expensive model evaluations when accumulated drift stays below a fixed threshold $τ$. This threshold controls an apparent tradeoff - raising it yields faster generation at the cost of visual quality, while lowering it preserves quality but sacrifices speed. We show this tradeoff is not fundamental; it is an artifact of holding $τ$ constant throughout denoising. We identify the existence of critical steps - timesteps where the drift signal changes rapidly - and show that applying a low threshold selectively at these steps while caching aggressively elsewhere recovers most of the quality of conservative caching at substantially higher inference speeds. Building on this insight, we propose ACID, a lightweight, training-free wrapper that monitors the rate of change of each method's existing drift signal to dynamically switch between a low and a high threshold. ACID is signal-agnostic and modular: it requires no retraining and plugs directly into existing dynamic caching methods without modifying their core mechanisms. Evaluated across three caching methods (TeaCache, EasyCache, DiCache) and three open-source video diffusion models (HunyuanVideo, Wan 2.1, CogVideoX), ACID consistently expands the Pareto frontier of visual quality versus inference speed beyond what any fixed threshold achieves. In particular, on TeaCache and HunyuanVideo, ACID achieves up to 2.16x speedup over the no-caching baseline, and up to 38% additional speedup over the conservative fixed-threshold baseline with negligible (<0.3 dB PSNR, <0.01 SSIM, <0.01 LPIPS) quality degradation.

---

## 论文详细总结（自动生成）

# 论文总结：ACID: Adaptive Caching for vIDeo generation

## 1. 论文的核心问题与整体含义（研究动机和背景）
视频扩散模型（如 HunyuanVideo、Wan 2.1、CogVideoX）能生成高质量视频，但推理速度极慢（生成约5秒视频在单张A100 GPU上需3~23分钟），主要瓶颈在于顺序去噪过程中对扩散主干网络的反复调用。  
现有训练无关的缓存加速方法（如 TeaCache、EasyCache、DiCache）通过累积“漂移信号”（drift signal），当累积漂移低于固定阈值 τ 时跳过完整模型计算并重用之前的输出。这种做法表面上面临一个权衡：提高 τ 则缓存更激进、速度更快但质量下降；降低 τ 则质量好但速度慢。  
论文的核心贡献在于指出**该权衡并非本质性的**，而是由于使用了固定阈值所致。相反，去噪过程存在**关键步骤**（critical steps），即漂移信号快速变化的时间步，在这些步骤上需要保守缓存，而在稳定区域可以激进缓存。通过自适应调整阈值，可以同时获得较好的速度与质量，扩展帕累托前沿。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 核心思想
- **自适应双阈值策略**：维持两个阈值——低阈值 τ_low（关键步骤使用，保守缓存）和高阈值 τ_high（稳定步骤使用，激进缓存）。  
- **关键步骤识别**：利用各缓存方法自身信号（如 TeaCache 的 ΔTEMNI、EasyCache 的 k×Δinput/‖v_{t-1}‖、DiCache 的 Δprobe features）的**变化率**（具体使用二阶导数/当前信号与前一时刻的差值绝对值）。当变化率超过稳定性边界 δ 时，认为处于动态/关键阶段，使用 τ_low；否则使用 τ_high。

### 算法流程（文字说明）
1. 初始化累积漂移 Σ=0，前一步信号 s_prev=None。
2. 对每个去噪时间步 t（从 T 到 0）：
   - 若处于热身阶段，运行完整模型并更新缓存方法状态。
   - 否则：
     - 计算当前信号 s_t。
     - 判断稳定性：若 s_prev 不为 None 且 |s_t - s_prev| ≤ δ，则当前步稳定，使用 τ_high；否则不稳定，使用 τ_low。
     - 将 s_t 加入累积漂移 Σ。
     - 若 Σ ≤ 当前选定的阈值，则跳过完整模型，通过缓存方法的重用机制重建输出。
     - 否则运行完整模型、更新状态、重置 Σ=0。
     - 保存 s_prev = s_t。

### 技术细节
- **模块化/信号无关**：ACID 作为轻量级外挂包装器，不修改原有缓存方法的核心机制，只需读取其现有信号即可工作。
- **无需训练**：所有操作均基于推理时计算，无额外训练开销。

## 3. 实验设计：数据集/场景、benchmark、对比方法
### 数据集与场景
- 使用 **VBench** 评测集的 33 个提示词（11 组，每组 3 个提示，覆盖所有 16 个 VBench 维度）。
- 每个配置使用固定种子生成 33 个视频。

### Benchmark & 对比方法
- **基线**：无缓存的原始模型推理。
- **对比方法**：固定阈值版本的 TeaCache、EasyCache、DiCache（多个阈值值域覆盖低到高）。
- **ACID 变体**：多组低/高阈值对及稳定性边界 δ 的配置。
- **评估维度**：推理延迟（latency）与速度提升（speedup = 无缓存延迟/方法延迟），视觉质量（LPIPS、PSNR、SSIM，均与无缓存基线对比）。

### 模型覆盖
- **HunyuanVideo T2V 720P**
- **Wan 2.1 T2V 1.3B**
- **CogVideoX 1.5-5B**（仅用于 TeaCache，因其他方法无官方实现）

## 4. 资源与算力
论文未明确说明总训练时长或 GPU 数量。但提及所有实验均在 **NVIDIA A100 80GB PCIe GPU** 上使用 PyTorch 完成。每项实验（每个 method × model × threshold 配置）基于固定种子生成 33 个视频，涉及 7 个方法-模型组合 × 多个阈值配置，总计算量未量化。

## 5. 实验数量与充分性
- **实验数量**：包含 7 个方法-模型组合（TeaCache×3模型、EasyCache×2模型、DiCache×2模型），每个组合测试了多个固定阈值和多个自适应配置，附录中列出了数十组实验数据（如 TeaCache on HunyuanVideo 有 5 种固定阈值 + 4 种自适应）。
- **消融实验**：通过对比固定阈值最佳点与自适应点，验证了自适应机制的有效性；通过实验证明最优转换点与信号二阶导数稳定时刻一致，为 δ 的选择提供了理论依据。
- **充分性评估**：实验较为充分，覆盖了三种主流动态缓存方法和三种代表性视频扩散模型（含不同架构如 DiT-based、U-Net-based）。但 EasyCache 和 DiCache 未在 CogVideoX 上评估（官方不支持），可能限制泛化结论。另外，仅在 33 个提示词上测试，样本量较小，且未包含真实用户评估或更广泛的应用场景。

## 6. 论文的主要结论与发现
- 固定阈值是导致速度-质量权衡的根本原因；使用自适应双阈值可以打破该权衡。
- ACID 在所有测试组合上均扩展了帕累托前沿，即在相同速度下获得更好质量，或在相同质量下获得更快速度。
- 具体数据：在 TeaCache + HunyuanVideo 上，ACID 实现 **2.16× 加速**（相对于无缓存），相比保守固定阈值（τ=0.1）额外提速 **38%**，而质量下降可忽略（PSNR < 0.3 dB, SSIM < 0.01, LPIPS < 0.01）。
- 自适应阈值的效果主要源于在关键步骤（信号变化剧烈处）采用低阈值保留质量，在稳定区域采用高阈值提升速度。

## 7. 优点
- **轻量无训练**：无需重新训练或修改模型，直接作为包装器使用，兼容现有动态缓存方法。
- **信号无关**：仅依赖各方法已有的漂移信号，不引入新计算开销或额外的监督信号。
- **普遍有效**：在三种不同缓存方法和三种不同模型上均验证了性能提升，表明发现具有一般性。
- **理论洞察**：发现了“关键步骤”的存在及其与信号二阶导数的关系，为自适应阈值提供了直观且有力的理由。
- **开源可复现**：详细列出了所有配置及结果，易于复现和进一步研究。

## 8. 不足与局限
- **实验覆盖有限**：仅涉及 33 个提示词，且仅在一个种子下生成，可能存在随机偏差。未在更大规模或更多样化数据集（如 UCF-101、DAVIS）上测试。
- **模型覆盖不完全**：EasyCache 和 DiCache 未在 CogVideoX 上评估，无法确认其在不同架构上的普遍性。
- **超参数敏感性**：稳定性边界 δ 和阈值对 (τ_low, τ_high) 的选择对性能有影响，论文未提供自动选择机制，用户需手动调参。
- **缺乏真实用户研究**：客观指标（PSNR、SSIM、LPIPS）虽能反映与无缓存基线的差异，但未必完全对应于人类感知质量，未做主观评测。
- **计算资源消耗未量化**：未报告每个配置的额外运行时开销（如信号计算、阈值切换等），尽管声称轻量，但未提供具体数字。
- **未考虑与其他加速方法协同**：如稀疏注意力、蒸馏等，未探讨与 ACID 组合的效果。

（完）
