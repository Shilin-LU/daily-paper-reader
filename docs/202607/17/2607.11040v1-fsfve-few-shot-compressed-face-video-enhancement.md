---
title: "FSFVE: Few Shot Compressed Face Video Enhancement"
title_zh: "FSFVE: 少样本压缩人脸视频增强"
authors: "Varun Ramesh Jois, Antonella DiLillo, James Storer"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11040v1"
tags: ["query:video-refine"]
score: 9.0
evidence: 少样本视频增强用于生成视频精炼
tldr: 视频通话中带宽限制导致压缩视频质量下降。本文提出FSFVE框架，仅需10帧人脸图像即可在100秒内训练一个增强模型，可在通话前或通话中训练，作为上层代理拦截并增强视频帧，无需修改应用程序。模型能在笔记本CPU上实时运行，显著提升压缩人脸视频的定量和感知质量，并已开源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.11040v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 765, \"height\": 618, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11040v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1282, \"height\": 1026, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.11040v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1447, \"height\": 1342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11040v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1074, \"height\": 131, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11040v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1212, \"height\": 294, \"label\": \"Table\"}]"
motivation: 解决带宽限制下视频通话质量差的问题，利用少量人脸帧快速训练个性化增强模型。
method: 基于FSFVE框架，用10帧人脸图像快速训练轻量级增强模型，作为上层拦截并优化压缩视频帧，支持实时推理。
result: 在典型笔记本CPU上实时运行，PSNR和感知质量均显著提升，仅需10帧训练即可生效。
conclusion: 提出一种无需修改应用的视频增强方案，适用于低带宽场景，具有实际部署价值。
---

## 摘要
视频通话已成为当今世界一种流行的通信形式，许多公司为此提供免费服务。然而，由于带宽限制，全球仍有数百万人体验着低质量的视频通话。尽管大多数人拥有所需的硬件。本文提出了一种增强高度压缩视频通话的新框架。我们证明，只需使用10帧人脸图像，即可在不到100秒的时间内快速训练一个模型来增强该视频通话实例。该模型可以在通话之前或期间进行训练，通过生成更高质量的视频来增强后续通话。视频会议应用程序无需修改——我们的系统可作为上层覆盖层，快速训练后让视频会议应用程序（如Zoom）照常运行，系统在图像显示前对其进行拦截和增强。该模型设计为可在低计算设备（如典型笔记本电脑CPU）上实时运行。实验表明，该模型在定量和感知上均显著提高了压缩人脸视频的质量。代码可在https://github.com/varun-jois/FSFVE找到。

## Abstract
Videocalling has become a popular form of communication in the world today, with many companies providing free services for it. However, there are still millions of people around the world that experience poor quality videocalls due to limitations in bandwidth. This despite, most people having the required hardware. In this paper we present a novel framework for enhancing highly compressed videocalls. We show, that with as little as 10 frames of the face, we can rapidly (in under 100 seconds) train a model to enhance that instance of the videocall. The model can be trained either prior to or during the call, enhancing the rest of the call by producing better quality video. The video conferencing application need not be modified - it can be off the shelf with our system as a layer on top that trains quickly then simply lets the video conferencing application (e.g. Zoom) run as usual, where our system intercepts and improves images before they are displayed. The model is designed to run in realtime on low-compute devices such as a typical laptop CPU. Experimentally, we show that the model significantly improves quality of compressed face video both quantitatively as well as perceptually. Code can be found at https://github.com/varun-jois/FSFVE.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：全球大量用户因带宽限制，在视频通话中经历高度压缩、质量低下的画面（如模糊、块效应、细节丢失），而现有增强模型要么无法实时运行在CPU上（需GPU），要么速度过慢，难以部署到普通笔记本电脑或手机等低算力设备。
- **研究动机**：能否针对**单个视频通话实例**，利用高度相关的通话帧，构建一个小而快的模型，在通话前或通话开始时用极少量样本（少样本学习）快速训练，从而在通话过程中实时增强视频质量。
- **整体含义**：提出一个无需修改现有视频会议应用（如Zoom）的轻量级上层系统，通过拦截并增强接收端的帧，显著提升低带宽下的通话体验。

### 2. 论文提出的方法论
#### 核心思想
- 利用视频通话中帧间高度相关性，针对特定人脸实例训练一个**轻量级MLP**，输入为图像的DCT块，输出为残差（残差连接到输入后经IDCT得到增强帧）。模型在频域学习映射，可快速收敛，推理时通过大步长（DCT块大小）实现CPU实时。

#### 关键技术细节
- **输入处理**：将输入帧划分为 **8×8 非重叠块**，对每个块进行2D DCT，每通道8×8系数，3通道展平为192维向量。
- **模型结构**：**3层隐藏层MLP**，每层512个神经元，使用**正弦激活函数**（SIREN），能更好捕捉高频细节；残差连接：`output = MLP(input) + input`。
- **损失函数**：**频域焦点损失**（Frequency Domain Focal Loss），对每个DCT系数按JPEG量化矩阵的倒数加权，强调低频分量（对感知质量影响更大），加速收敛。
- **训练方式**：少样本学习，仅用5~30帧（文中多数实验用30帧），使用RAdam优化器，学习率1e-4，训练100 epochs（<100秒）。可在通话前（预取高质量帧）或通话开始时训练。

#### 算法流程（文字说明）
1. 获取若干高质量人脸帧（如通话开始前的几秒视频），用BlazeFace检测、对齐并裁剪为256×256。
2. 将高质量帧用相同压缩参数（H.264/H.265，CRF值）重新编码为低质量帧，作为输入；原始高质量帧作为目标。
3. 对每对输入-目标帧，提取8×8非重叠DCT块，向量化后作为MLP输入/输出。
4. 使用频率焦点L1损失训练MLP（预测残差），100 epochs。
5. 推理时：对每帧进行相同分块→DCT→MLP（加残差）→IDCT→重组，实时输出增强帧。

### 3. 实验设计
- **数据集**：**DeepFakeDetection**公共数据集，选取其中“talking against wall”类别（模拟视频通话场景），共27个视频，每个约972帧。
- **预处理**：人脸检测（BlazeFace）、对齐（面部关键点）、裁剪256×256。使用k-means聚类（k=训练帧数）选取最代表性帧作为训练集，保证多样性。
- **压缩模拟**：使用**H.264**和**H.265**编码器，CRF值设为**36、40、44**（越高压缩越严重，损失越大，通常17-28为可接受，实验模拟低带宽条件）。
- **对比方法**：
  - 基线：压缩后的原始视频。
  - ResNet：极小型基础ResNet（5残差块，8通道，内核大小3，约6k参数）。
  - RTFVE-0：修改自RTFVE（去掉参考图像，使用深度可分离卷积，约102k参数）。
  - FSFVE（本文）：约1M参数，但推理快（30.24 FPS在CPU上）。
- **评价指标**：PSNR、SSIM（失真度量）、LPIPS（感知度量）。
- **消融实验**：对比空间域（RGB块直接输入）、频域等权重、频域+焦点损失。

### 4. 资源与算力
- 文中**未明确指出GPU型号或数量**，但描述了训练在**CPU**上进行（训练时间<100秒），推理在典型笔记本CPU上实时（30.24 FPS）。模型大小约3.5 MB。训练消耗算力较低，适合移动端或低配设备。

### 5. 实验数量与充分性
- **主要实验**：对两种编码标准（H.264/H.265）、三种CRF水平（36/40/44），对比三种方法（基线、ResNet、RTFVE-0），报告3个指标（PSNR/SSIM/LPIPS），共3×2×3×3=54组数据（如表1覆盖）。
- **训练集大小影响**：表2，使用5/10/20/30帧训练，CRF 44下PSNR对比，展示了少样本的鲁棒性。
- **消融实验**：表3，空间域 vs 频域等权 vs 频域+焦点损失，验证每个组件的贡献。
- **定性结果**：给出两个视频示例的增强前后对比图（图2）。
- **充分性评价**：实验设计较全面，覆盖不同压缩程度、不同编码标准，消融实验清晰，对比方法同为CPU实时模型（ResNet, RTFVE-0），公平性较好。但缺少与更多先进模型（如GAN-based）的对比（因它们无法CPU实时）。总体充分客观。

### 6. 论文的主要结论与发现
- **FSFVE显著提升压缩视频质量**：在H.264 CRF 44下，PSNR提高1.51 dB，SSIM上涨0.0266，LPIPS降低0.0468（相比基线）；对于更高压缩场景提升更明显（BD-Rate -25.12%，BD-PSNR 1.3253 dB）。
- **少样本有效**：仅10帧训练即可提升>1 dB PSNR；30帧训练效果更好。
- **频域焦点损失加快收敛**：相比等权频域损失再提升约0.3 dB PSNR。
- **实时CPU可行性**：模型在CPU上达到30.24 FPS，满足24 FPS视频通话实时要求。
- **跨编解码器通用**：H.264和H.265下均有类似提升。

### 7. 优点（方法或实验设计亮点）
- **少样本+快速训练**：仅需10-30帧即可训练，训练时间<100秒，实用性强。
- **CPU实时推理**：模型参数量约1M但仍保持高速，得益于DCT块的步长和MLP的简单性。
- **无需修改应用**：作为上层代理，兼容现有视频通话软件。
- **频域残差学习**：结合正弦激活函数和焦点损失，有效捕捉高频细节，加速收敛。
- **实验全面**：覆盖两种主流编码、多级压缩、多指标、消融及训练集大小分析，对比方法均限于同等级实时模型，公平合理。

### 8. 不足与局限
- **仅针对人脸视频**：方法依赖人脸检测和对齐，不适用于非人脸场景（如屏幕共享、全身视频）。
- **需要初始高质量帧**：在通话开始前或开始时需获取几秒高质量视频，对于突发通话或无法预取的情况不够灵活。
- **训练时间限制**：100秒训练对极短通话（<2分钟）可能不划算，且训练期间需计算资源。
- **缺乏与更强模型对比**：没有与需GPU的先进模型（如GFP-GAN、VQFR）比较性能上限，尽管因实时性要求可理解。
- **数据集单一**：仅用DeepFakeDetection的一个子类（27个视频），样本量偏小，且主要为室内、正面/半正面视角，泛化性待验证。
- **未评估实际网络传输延迟**：系统作为拦截层可能引入额外延迟（虽推理实时，但训练和模型传输未讨论）。
- **定量增益幅度**：PSNR提升约1~1.5 dB，SSIM提升约0.02~0.03，虽然统计显著，但实际主观提升可能有限（尤其是低压缩场景）。

（完）
