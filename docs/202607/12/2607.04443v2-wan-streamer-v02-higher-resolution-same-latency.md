---
title: "Wan-Streamer v0.2: Higher Resolution, Same Latency"
title_zh: "Wan-Streamer v0.2: 更高分辨率，相同延迟"
authors: "Lianghua Huang, Zhi-Fan Wu, Yupeng Shi, Wei Wang, Mengyang Feng, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuxiang Bao, Yuzheng Wang, Zoubin Bi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04443v2"
tags: ["query:fast-gen"]
score: 8.0
evidence: 实时视频流模型，低延迟高分辨率
tldr: 实时音视频交互模型面临分辨率与延迟的权衡。Wan-Streamer v0.2采用thinker-performer架构，thinker单GPU处理感知与缓存构建，performer多GPU上下文并行生成高分辨率潜变量。输出分辨率从192x336提升至640x368，模型侧延迟保持约200ms，总延迟约550ms。在不增加用户感知延迟的前提下显著提升视觉清晰度，支持更丰富的场景理解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04443v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 775, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04443v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04443v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 658, \"label\": \"Table\"}]"
motivation: 提升交互视频分辨率至640x368，同时保持约200ms低延迟，以支持场景中景代理的实时理解。
method: 保持v0.1公式，thinker单GPU低延迟通路，performer多GPU Ulysses上下文并行分片生成高分辨率视频潜变量。
result: 输出分辨率提升至640x368，模型侧延迟约200ms，总延迟约550ms（含350ms网络预算）。
conclusion: 实现了分辨率提升与延迟不变，增强了实时对话中视觉细节的可辨性。
---

## 摘要
我们推出了Wan-Streamer v0.2，这是对原生流式、端到端音视频交互模型的一次延迟保持升级。v0.2保留了v0.1的建模公式，但将交互输出流从192x336提升至640x368，同时保持约200毫秒的模型端信号到信号延迟（25 FPS）。更高分辨率的流支持场景接地中景智能体，在实时对话过程中，其姿态、视线、双手、附近物体以及局部场景布局保持清晰可辨。为了在支持更大视觉流的同时不增加用户可见延迟，v0.2保留了thinker作为单GPU低延迟路径，用于流式感知、构建生成缓存的短语言/状态Transformer传递，以及最终解码。performer则成为一个多GPU的Ulysses风格上下文并行组，用于昂贵的下一单元潜变量生成。每个performer rank将传入的K/V写入预先分片的本地缓存。长的高分辨率潜视频序列在各rank之间分片进行去噪，并通过Ulysses通信进行汇聚，而较短的音频潜序列则不进行序列分片生成。在这种划分下，thinker的语言/状态计算仅以K/V条件形式到达performer，因此不需要在performer组内部传输单独的语言序列。这使额外硬件集中于视觉生成，同时保持紧凑的thinker-performer边界，在包含350毫秒双向网络预算的情况下，总远程交互延迟约为550毫秒。

## Abstract
We present Wan-Streamer v0.2, a latency-preserving upgrade of the native-streaming, end-to-end audio-visual interaction model. v0.2 keeps the v0.1 modeling formulation, but raises the interactive output stream from 192x336 to 640x368 while preserving approximately 200 ms model-side signal-to-signal latency at 25 FPS. The higher-resolution stream supports scene-grounded mid-shot agents whose posture, gaze, hands, nearby objects, and local scene layout remain legible during real-time conversation. To support the larger visual stream without adding user-visible delay, v0.2 keeps the thinker as a single-GPU low-latency path for streaming perception, the short language/state Transformer pass that builds the generation cache, and final decoding. The performer becomes a multi-GPU Ulysses-style context-parallel group for the expensive next-unit latent generation. Each performer rank writes incoming K/V into a pre-sharded local cache. The long high-resolution latent video sequence is split across ranks for denoising and gathered through Ulysses communication, while the much shorter audio latent sequence is generated without sequence sharding. In this split, the thinker's language/state computation reaches the performer only as K/V conditioning, so no separate language sequence has to be communicated inside the performer group. This concentrates additional hardware on visual generation while preserving the compact thinker-performer boundary, keeping total remote interaction latency at approximately 550 ms when a 350 ms bidirectional network budget is included.

---

## 论文详细总结（自动生成）

# Wan-Streamer v0.2 论文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：实时音视频交互模型（如Wan-Streamer v0.1）在输出分辨率（192×336）与低延迟（约200ms模型侧）之间存在根本性权衡。低分辨率限制了视觉内容的丰富性，无法支持场景接地中景智能体（如姿态、手势、物体等细节可见的远程实时对话）。
- **背景**：全双工对话、多模态感知、流式视频生成等方向已取得进展，但将感知、响应、同步音视频生成集成在一个因果流中，并保持低延迟，仍具挑战。
- **目标**：将输出分辨率从192×336提升至640×368（25 FPS），同时保持模型侧信号到信号延迟约200ms，总远程交互延迟约550ms（含350ms双向网络预算），从而支持更清晰的中景对话场景。

## 2. 方法论：核心思想、关键技术与算法流程
- **核心思想**：保持v0.1原生流式公式（用户文本、音频、视频在同一因果时间线上，由块因果注意力协调），但将高分辨率视觉生成的计算负担转移到多GPU并行架构，而不影响延迟关键路径。
- **关键技术**：
  - **Thinker**（单GPU）：负责流式感知、语言/状态更新、KV缓存构建、因果解码。该路径是延迟关键环路，保持紧凑低延迟。
  - **Performer**（多GPU Ulysses风格上下文并行组）：负责高成本潜变量生成。每个performer rank拥有预分片的本地KV缓存；长的视频潜序列在rank间分片，通过Ulysses all-to-all/gather通信进行注意力计算；短的音频潜序列不分片。
  - **通信**：thinker将performer兼容的K/V切片广播给所有performer rank；performer组内Ulysses通信内部完成，不需要额外传输语言序列。
- **算法流程（每160ms单元）**：
  1. user观察被thinker编码，更新状态，生成新的K/V切片。
  2. thinker将K/V切片广播给performer。
  3. performer更新各rank的本地分片缓存，运行上下文并行去噪生成下一单元的潜视频（分片）和潜音频（不分片）。
  4. thinker接收前一步生成的潜变量，解码并输出音视频响应。
- **关键约束**：performer组时间 + 通信时间需小于160ms一个单元。

## 3. 实验设计：数据集、基准与对比方法
- **数据集/场景**：未提及具体数据集。使用生成式对话的定性观察，聚焦于听/说过程中视觉稳定性和可辨性（面部细节、手势、物体、场景布局）。
- **基准**：仅与v0.1自身对比（192×336 vs. 640×368，延迟目标相同）。
- **对比方法**：未与其他同类系统（如StreamAvatar、Moshi等）进行定量对比。仅提及公共系统使用不同端点（如首包延迟、首帧延迟等），但本文采用与v0.1一致的测量惯例。
- **评估指标**：模型侧延迟（近似200ms）、总交互延迟（近似550ms），以及定性视觉观察。无量化图像/视频质量指标（如FID、FVD、PSNR等），也无音频质量量化。

## 4. 资源与算力
- **明确内容**：
  - Thinker：1 GPU。
  - Performer：多GPU Ulysses上下文并行组（具体数量未说明）。
- **未提及**：
  - GPU型号（如A100/H100）、总GPU数量、训练时长、训练数据规模等均未在论文中给出。只涉及推理部署配置，未提训练资源。

## 5. 实验数量与充分性
- **实验数量**：非常有限。仅进行了单次延迟测量（声称约200ms，假设350ms网络得出总约550ms），以及定性视觉观察（几段对话样本）。
- **充分性评估**：不充分。缺乏系统性的消融实验（如不同分辨率、不同并行策略、不同网络条件）、定量标准指标评估（多模态生成质量）、用户研究、鲁棒性测试。客观性不足，因为延迟数据依赖于特定假设（网络预算），且无与其他方法复现对比。
- **公平性**：仅与自身v0.1比较，无外部对照，无法判断相较于其他实时交互系统的优劣。

## 6. 主要结论与发现
- 成功将Wan-Streamer输出分辨率从192×336提升至640×368，模型侧延迟仍保持约200ms，总交互延迟约550ms。
- 通过thinker-performer分离架构，将高分辨率潜生成计算集中到多GPU并行组，不影响延迟关键路径。
- 更高分辨率支持场景接地中景智能体，增强了对话中姿态、物体、场景布局的可读性。

## 7. 优点：方法与实验设计亮点
- **创新架构设计**：thinker单GPU保持低延迟，performer多GPU通过Ulysses上下文并行吸收额外计算；预分片的KV缓存和只传输K/V条件避免了额外语言序列通信。
- **延迟保持不变**：实现分辨率显著提升而不增加用户感知延迟，是工程优化的重要贡献。
- **保持原生流式公式**：延续v0.1的端到端因果流，简化了交互逻辑。
- **清晰的boundary定义**：将模型侧延迟与网络延迟分开测量，便于部署分析。

## 8. 不足与局限
- **缺乏定量实验**：无任何客观质量指标（如图像/视频保真度、音频同步、理解准确率等），仅凭定性观察不足以证明生成质量。
- **实验设置不透明**：未报告performer GPU数量、网络通信开销、不同分辨率下的延迟分布、系统鲁棒性（如丢包、网络抖动）等关键信息。
- **无外部对比**：未与现有实时交互系统（如Moshi、StreamAvatar等）在同等条件下对比，无法体现相对优势。
- **网络预算假设**：总延迟550ms依赖350ms双向网络预算，实际网络环境可能不同，影响结果泛化。
- **应用限制**：依赖多GPU Ulysses通信，部署成本高；仅适用于流式场景，不适用于离线生成或单GPU环境；训练部分未被探讨（可能同v0.1，未重新训练？）。
- **偏见风险**：定性观察由作者自行评估，可能存在确认偏倚；未包含用户主观评价。

（完）
