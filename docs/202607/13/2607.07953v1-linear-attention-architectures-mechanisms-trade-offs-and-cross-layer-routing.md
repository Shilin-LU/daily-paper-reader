---
title: "Linear Attention Architectures: Mechanisms, Trade-offs, and Cross-Layer Routing"
title_zh: 线性注意力架构：机制、权衡与跨层路由
authors: "Tommaso Cerruti, Tim Rieder, George Rowlands, Lingfeng Jin, Imanol Schlag"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07953v1"
tags: ["query:fast-gen"]
score: 8.0
evidence: 线性注意力架构的比较研究，可用于高效生成
tldr: 自注意力机制因二次复杂度限制了长序列训练。本文对比了softmax注意力与四种线性循环注意力架构（DeltaNet等），以统一记忆记法揭示其差异。在350M参数15B token实验中，Kimi Delta Attention+Muon验证损失最低，纯Gated DeltaNet+AdamW训练吞吐最高。Muon相比AdamW一致降低损失。提出了跨层路由机制（CLVR），将下层写入值转发至上层可适度改进损失。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07953v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1463, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07953v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1369, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07953v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1696, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07953v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1698, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07953v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1023, \"height\": 631, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07953v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1453, \"height\": 790, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07953v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1663, \"height\": 1142, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1720, \"height\": 997, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 897, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1709, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1709, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1081, \"height\": 442, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1125, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1709, \"height\": 397, \"label\": \"Table\"}]"
motivation: 自注意力二次复杂度限制长序列处理，需要更高效的线性注意力变体，并探索跨层记忆交互。
method: 用统一循环记忆记法表达DeltaNet等四种机制，并在350M参数模型上比较，引入跨层值路由（CLVR）。
result: Kimi Delta Attention+Muon验证损失最低；纯Gated DeltaNet+AdamW吞吐最高；Muon优于AdamW；CLVR适度改进损失。
conclusion: 线性注意力架构在效率和效果间存在权衡，跨层路由有潜力但需进一步优化设计。
---

## 摘要
自注意力机制允许每个token从整个上下文中检索信息，但其序列长度的二次成本限制了长上下文的训练和推理。本文对softmax注意力与四种近期提出的循环线性注意力架构进行了比较研究：DeltaNet、Gated DeltaNet、Kimi Delta Attention和Gated DeltaNet-2。我们用一种通用的循环记忆符号表示这些机制，明确它们在表现力、记忆衰减、擦除和写入控制、训练吞吐量以及实现复杂度方面的差异。我们的实验以350M参数模型为中心，训练了150亿个token，包括优化器和学习率比较、混合与纯架构堆栈比较、序列长度运行时测量、1.3B和3B参数的大规模DeltaNet运行以及少量下游评估。报告的速度结果衡量训练吞吐量和迭代时间；我们不提供经验性推理速度基准。在所报告的350M参数、150亿token的扫描范围内，使用Muon的Kimi Delta Attention达到了最低的最终验证损失，使用AdamW训练的纯Gated DeltaNet堆栈具有最高的归一化训练吞吐量，混合堆栈通常以吞吐量成本改善损失，而在我们评估的匹配架构设置中，Muon相对于AdamW始终降低了最终验证损失。我们为DeltaNet风格的内存引入了轻量级跨层路由机制并进行评估。最自然的受DeltaNet启发的公式，即向前一层delta规则的写入错误传递到下一层的值目标，并没有比匹配的基线有所改进。路由到对齐的隐藏流并转发写入值，在我们报告的匹配运行中产生了适度的改进：跨层值路由（CLVR）降低了DeltaNet和Gated DeltaNet的最终验证损失。

## Abstract
Self-attention lets each token retrieve information from the full context, but its quadratic cost in sequence length limits training and inference at long context. This paper presents a comparative study of softmax attention and four recent recurrent linear-attention architectures: DeltaNet, Gated DeltaNet, Kimi Delta Attention, and Gated DeltaNet-2. We express these mechanisms in a common recurrent-memory notation, making explicit how they differ in expressivity, memory decay, erase and write control, training throughput, and implementation complexity. Our experiments center on 350M-parameter models trained for 15B tokens, and include optimizer and learning-rate comparisons, hybrid-versus-pure stack comparisons, sequence-length runtime measurements, larger DeltaNet runs at 1.3B and 3B parameters, and a small set of downstream evaluations. The reported speed results measure training throughput and iteration time; we do not provide an empirical inference-speed benchmark. Within the reported 350M-parameter, 15B-token sweep, Kimi Delta Attention with Muon reaches the lowest final validation loss, a pure Gated DeltaNet stack trained with AdamW has the highest normalized training throughput, hybrid stacks generally improve loss at a throughput cost, and Muon consistently lowers final validation loss relative to AdamW in the matched architecture settings we evaluate. We introduce and evaluate lightweight cross-layer routing mechanisms for DeltaNet-style memories. The most natural DeltaNet-inspired formulation, forwarding a lower layer's delta-rule write error into the next layer's value target, does not improve over matched baselines. Routing into the aligned hidden stream and forwarding the write value instead yields a modest improvement in the matched runs we report: Cross-Layer Value Routing (CLVR) lowers final validation loss for both DeltaNet and Gated DeltaNet.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：Transformer 中的自注意力机制具有序列长度的二次复杂度（O(T²)），导致长上下文训练和推理成本高昂。虽然后续提出的线性注意力通过循环记忆降低为线性复杂度，但早期的线性注意力在准确率上有显著损失。近年来，DeltaNet 等基于增量规则（delta rule）的变体逐步缩小了与 softmax 注意力的差距，但不同设计在表达力、遗忘/写入控制、吞吐量等方面存在复杂权衡。此外，深层模型存在信息稀释问题（lower-layer 信号难以被高层恢复），现有跨层方案（如 Attention Residuals）应用于线性循环结构时会部分破坏其效率优势，需要更轻量的跨层信息共享方法。
- **整体含义**：本文旨在系统比较 softmax 注意力与四种主流的线性循环注意力架构（DeltaNet、Gated DeltaNet、Kimi Delta Attention、Gated DeltaNet-2），并探索一种保持线性时间结构的跨层路由机制。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
### 核心思想
- **统一循环记忆表示**：将所有线性注意力机制表达为具有固定大小记忆矩阵 W 的循环更新，通过特征映射（ϕ）将查询和键映射到共享维度，并用增量规则（delta rule）写入残差，以降低键-值关联之间的干扰。
- **跨层路由**：针对 DeltaNet 类内存，提出将下层循环记忆层的内部信号（写入值或写入误差）通过零初始化投影注入到上层的共享残差流（hidden stream），而非注入到上层特定层的值空间，以解决空间不匹配问题。

### 关键技术细节
- **基本更新框架**：
  - 对于每个 token i：查询 q(i)、键 k(i)、值 v(i)；特征映射后的键 κ = ϕ(k)。
  - 记忆预测： `v̄(i) = W(i-1) κ(i)`
  - 增量残差： `r(i) = v(i) - v̄(i)`
  - 写入更新： `W(i) = W(i-1) + β r(i) ⊗ κ(i)` （β 为写入强度标量）
- **各变体的差异**：
  - **DeltaNet**：基本增量更新，无显式遗忘机制。
  - **Gated DeltaNet**：添加标量遗忘门 α（对整个记忆进行衰减），写入前先将旧记忆乘以 α。
  - **Kimi Delta Attention**：用向量遗忘门（通道级），沿键特征维度对不同通道施加不同衰减。
  - **Gated DeltaNet-2**：进一步解耦为通道级擦除门（erase gate, b）和写入门（write gate, w），分别控制从记忆中移除的内容和写入的新值。
- **跨层路由（CLVR）**：
  - 定义下层路由信号 `s(l,t)`：写入值 `v(l,t)` 或写入误差 `r(l,t)`。
  - 通过可学习的零初始化投影矩阵 `P_l` 将 `s(l,t)` 投影到模型维度，加到残差流：`h(l,t) ← h(l,t) + P_l s(l,t)`。
  - 零初始化确保训练开始时路由贡献为零，模型从基线出发学习是否路由。
  - 最佳配置为路由*写入值*（而非写入误差），且注入到共享残差流（而非目标层的值空间）。

### 核心算法流程（文字说明）
1. 输入 token 序列，对每个位置 i 生成 q, k, v。
2. 使用特征映射 ϕ 得到键向量 κ。
3. 从当前记忆 W 预测值 v̄。
4. 计算残差 r = v - v̄。
5. 根据架构变体应用遗忘、擦除、写入门控，更新记忆 W。
6. 输出 `y = W ϕ(q)`。
7. 对于路由版本：在馈入上层前，将下层写入值投影后加到残差流。

## 3. 实验设计：数据集、Benchmark、对比方法
- **数据集**：FineWeb-Edu（来源：FineWeb 子集，LLaMA2 tokenizer）。
- **Benchmark**：验证损失（held-out cross-entropy，越低越好）；下游任务：HellaSwag、PIQA、WinoGrande（zero-shot 评估）。
- **对比方法**：
  - **基线 softmax attention**（标准 Transformer 注意力）
  - **四种线性循环注意力**：DeltaNet、Gated DeltaNet、Kimi Delta Attention、Gated DeltaNet-2
  - 每种架构评估了 **纯堆栈**（全部层使用线性注意力）和 **混合堆栈**（线性与softmax按 2:1 或 3:1 穿插）。
  - 额外比较 **优化器**：AdamW 与 Muon。
  - 学习率消融实验（不同 LR 值）。
  - 跨层路由消融：对比 CLER（值目标注入）、CLER-H（隐藏流注入写入误差）、CLVR（隐藏流注入写入值），以及无路由基线。

## 4. 资源与算力
- **硬件**：一个 GH200 节点（配备 4 个 GPU），使用 bf16 精度，在 CSCS Alps 系统上训练。
- **主要实验**：350M 参数模型，训练 150 亿 tokens，序列长度 4096，全局 batch size 128，微批次大小 16（主架构扫描）。
- **大规模 DeltaNet 实验**：1.3B 参数训练 400 亿 tokens，3B 参数训练 600 亿 tokens（均使用相同硬件配置？论文未明确说明 GPU 数量是否扩展，但推测使用多个节点）。
- **推理速度**：论文明确不提供推理速度基准，仅测量训练迭代时间。

## 5. 实验数量与充分性
- **实验数量**：
  - 主架构扫描：5 种架构 × 2 种堆栈（纯/混合）× 2 种优化器（AdamW/Muon）= 约 20 个主要运行（部分变体有缺失）。
  - 学习率消融：多种 LR 值（3e-4, 1e-3 等），每种组合训练 2000 步（约 10.5 亿 tokens）。
  - 序列长度缩放实验：4k/16k/32k。
  - 大规模实验：DeltaNet 在 1.3B/40B 和 3B/60B 下的多组 LR 运行。
  - 路由实验：DeltaNet 和 Gated DeltaNet 两个宿主 × 三个路由变体 × 三种规模（350M/1B, 350M/15B, 1.3B/40B），加上控制实验（惊喜门控、拼接路由、隐藏状态路由、参数扩张等）。
- **充分性评价**：
  - **优点**：覆盖了主要架构、优化器、学习率、堆栈模式、序列长度、跨层路由，实验设计较为系统。
  - **不足**：所有比较均为单次运行，未报告标准差或多次随机种子的平均结果；下游任务仅限三个基准（HellaSwag, PIQA, WinoGrande），缺乏长上下文任务（如合成检索、passkey 等）；跨层路由仅测试了 DeltaNet 和 Gated DeltaNet，未在 Kimi Delta Attention 或 Gated DeltaNet-2 上评估；更大规模的 DeltaNet 路由实验缺少 DeltaNet 宿主（仅 Gated DeltaNet 有 1.3B/40B 结果）；优化器和学习率调参未完全均衡（部分配置可能未达到最佳 LR）。

## 6. 论文的主要结论与发现
1. **架构权衡**：不存在单一最优架构。在 350M/15B 设置下：
   - **最低验证损失**：Kimi Delta Attention + Muon + 混合堆栈（2.273）。
   - **最高训练吞吐量**：纯 Gated DeltaNet + AdamW（归一化为 100%）。
   - Muon 优化器在匹配架构中一致优于 AdamW（降低最终损失）。
   - 混合堆栈通常改善损失但降低吞吐，纯循环堆栈具有最好的长序列时间缩放特性（4k→32k，纯 Gated DeltaNet 仅增加 8% 迭代时间，softmax 增加 192%）。
2. **学习率敏感性**：不同优化器/架构组合的最佳学习率不同（Muon 偏好较低 LR ~ 3e-4；AdamW 线性注意力偏好 ~ 1e-3）。
3. **跨层路由**：
   - CLER（值目标注入写入误差）**无改善**。
   - CLER-H（隐藏流注入写入误差）有微小但一致的改善。
   - CLVR（隐藏流注入写入值）**优于 CLER-H**，且在所有报告单次运行中均降低验证损失（例如 350M/1B 下 DeltaNet 降低 0.0119，Gated DeltaNet 降低 0.0103），但增益随着训练 token 数增加或模型规模增大而缩小。
   - 控制实验表明增益来自路由的具体内容（写入值）而非额外参数。

## 7. 优点
- **系统性的统一符号框架**：将多种线性注意力以标准循环记忆记法表达，便于理解本质差异。
- **全面的实验覆盖**：同时比较了架构、优化器、学习率、堆栈模式、序列长度缩放、跨层路由，并进行了大规模验证（1.3B/40B, 3B/60B）。
- **清晰的消融链**：从 CLER 到 CLER-H 到 CLVR，逐步诊断空间不匹配和信号选择问题，逻辑严谨。
- **可重复性**：提供基于 Megatron 的实现和训练启动脚本（公开代码仓库），并列出关键配置表（Table 1）。
- **跨层路由的实用性**：CLVR 设计保持线性时间复杂度，零初始化投影使模型自然学习路由强度，无额外超参数。

## 8. 不足与局限
- **统计可靠性不足**：所有主要结果为单次运行，缺乏重复实验的标准差，小差异（< 1e-2）可能存在偶然性。
- **下游评估有限**：仅三个零样本任务（HellaSwag, PIQA, WinoGrande），未覆盖长上下文推理、记忆检索等场景，路由在任务层面的推广性未验证。
- **跨层路由未在所有架构上测试**：Kimi Delta Attention 和 Gated DeltaNet-2 被排除，而它们的内部信号结构可能更适合或需要不同的路由策略。
- **大规模路由实验不完整**：DeltaNet 在 1.3B/40B 规模缺少隐藏流路由结果；3B 规模未测试路由。
- **推理性能未测量**：论文明确不做推理速度分析，但实际部署中解码并行化、KV 缓存等影响因素未讨论。
- **超参数搜索可能不充分**：部分架构（如 Gated DeltaNet-2）可能未找到最佳学习率，导致性能偏低。
- **训练 token 范围有限**：350M 模型仅训练 15B tokens，更长时间训练下的趋势未知。

（完）
