---
title: "Wan-Streamer v0.2: Higher Resolution, Same Latency"
title_zh: Wan-Streamer v0.2：更高分辨率，相同延迟
authors: "Lianghua Huang, Zhi-Fan Wu, Yupeng Shi, Wei Wang, Mengyang Feng, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuxiang Bao, Yuzheng Wang, Zoubin Bi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04443v1"
tags: ["query:fast-gen"]
score: 8.0
evidence: 在单GPU上实现低延迟实时视频生成
tldr: 实时音视频交互中分辨率提升常伴随延迟增加的难题。Wan-Streamer v0.2维持思考器-执行器分离，思考器单GPU完成流式感知、语言/状态Transformer构建缓存与解码，执行器多GPU用Ulysses上下文并行对视频潜变量分片去噪、音频不分片，仅传K/V条件，将输出从192x336提至640x368且模型延迟保持在约200ms。结果端到端总延迟约550ms（含350ms网络），场景中姿势、目光、手部及局部细节在对话中清晰可辨。该工作为实时交互中高分辨率与低延迟的兼容提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04443v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 775}, {\"url\": \"assets/figures/arxiv/2607.04443v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 664}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04443v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 658}]"
motivation: 提升分辨率至640x368而不增加延迟，使场景接地中景代理的姿势、目光、手部细节在实时对话中清晰可辨。
method: 保持思考器-执行器分离架构，思考器单GPU处理流式感知与生成缓存，执行器多GPU使用Ulysses上下文并行对视频潜在序列分片去噪、音频不分片，通过K/V传递避免额外通信。
result: 输出分辨率从192x336提升至640x368，模型延迟保持约200ms，端到端延迟约550ms（含350ms网络），场景中姿势、目光、手部细节清晰可辨。
conclusion: 证明了在保持低延迟的同时提升分辨率的可行性，为高分辨率实时音视频交互提供了高效工程方案。
---

## 摘要
我们提出了Wan-Streamer v0.2，这是对原生流式端到端音视频交互模型的延迟保持升级。v0.2保持了v0.1的建模公式，但将交互输出流从192x336提升到640x368，同时在25 FPS下保持约200毫秒的模型端信号到信号延迟。更高分辨率的流支持场景锚定的中景智能体，其姿态、视线、手部、附近物体和局部场景布局在实时对话中保持清晰可辨。为了在不增加用户可见延迟的情况下支持更大的视觉流，v0.2将思考者保持为单GPU低延迟路径，用于流式感知、构建生成缓存的短语言/状态Transformer传递以及最终解码。执行者则成为一个多GPU的Ulysses风格上下文并行组，用于昂贵的下一单元潜在生成。每个执行者rank将传入的K/V写入预分片的本地缓存。长的、高分辨率的潜在视频序列在各个rank之间分割以进行去噪，并通过Ulysses通信进行汇聚，而更短的音频潜在序列则无需序列分片即可生成。在这种分割中，思考者的语言/状态计算仅作为K/V条件传递给执行者，因此在执行者组内无需通信单独的语言序列。这可以将额外的硬件集中在视觉生成上，同时保持紧凑的思考者-执行者边界，使得在包含350毫秒双向网络预算的情况下，总远程交互延迟约为550毫秒。

## Abstract
We present Wan-Streamer v0.2, a latency-preserving upgrade of the native-streaming, end-to-end audio-visual interaction model. v0.2 keeps the v0.1 modeling formulation, but raises the interactive output stream from 192x336 to 640x368 while preserving approximately 200 ms model-side signal-to-signal latency at 25 FPS. The higher-resolution stream supports scene-grounded mid-shot agents whose posture, gaze, hands, nearby objects, and local scene layout remain legible during real-time conversation. To support the larger visual stream without adding user-visible delay, v0.2 keeps the thinker as a single-GPU low-latency path for streaming perception, the short language/state Transformer pass that builds the generation cache, and final decoding. The performer becomes a multi-GPU Ulysses-style context-parallel group for the expensive next-unit latent generation. Each performer rank writes incoming K/V into a pre-sharded local cache. The long high-resolution latent video sequence is split across ranks for denoising and gathered through Ulysses communication, while the much shorter audio latent sequence is generated without sequence sharding. In this split, the thinker's language/state computation reaches the performer only as K/V conditioning, so no separate language sequence has to be communicated inside the performer group. This concentrates additional hardware on visual generation while preserving the compact thinker-performer boundary, keeping total remote interaction latency at approximately 550 ms when a 350 ms bidirectional network budget is included.

---

## 论文详细总结（自动生成）

# Wan-Streamer v0.2 论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：实时音视频交互系统需要在低延迟（~200ms模型端延迟）前提下提供清晰、场景化的视觉输出。Wan-Streamer v0.1虽然实现了端到端的流式交互，但输出分辨率仅为192×336，限制了视觉范围：近距离视频通话能保留面部反应，而中景构图下身体姿势、手部、附近物体和场景上下文因压缩而难以辨认。
- **核心问题**：如何在不增加用户可察觉延迟的前提下，将交互输出分辨率提升至640×368，同时支持场景锚定的中景智能体（agent），使其姿势、目光、手部、物体和场景布局在实时对话中清晰可辨。
- **整体含义**：该工作证明了在保持低延迟（~200ms模型端延迟、~550ms总交互延迟）的同时显著提升分辨率的可行性，为将实时音视频交互从“肖像式通话”扩展到“场景化对话”奠定了基础。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：保持Wan-Streamer v0.1的“原生流式”（native-streaming）建模公式（文本、音频、视频共享因果时间线，由单Transformer统一建模），但通过改变服务拓扑（serving topology）来容纳更高分辨率的生成成本：将模型拆分为**思考者（Thinker）**和**执行者（Performer）**两个角色，并在执行者组内采用Ulysses风格的上下文并行（context parallelism）处理高分辨率视频潜变量的去噪。
- **关键技术与细节**：
  - **思考者（Thinker）**：部署在单GPU上，负责流式感知（编码用户输入）、语言/状态Transformer更新、KV缓存构建，以及最终因果解码。该路径保持低延迟。
  - **执行者（Performer）**：多GPU组，使用Ulysses风格的上下文并行，专门处理昂贵的流匹配（flow-matching）潜变量生成。每个执行者rank维护预分片的KV缓存，将长的高分辨率视频潜变量序列在rank间分割去噪，通过Ulysses all-to-all/gather通信实现注意力计算；而音频潜变量序列较短，不分片。
  - **通信模式**：思考者仅将兼容执行者的K/V切片广播给执行者，不传递语言序列；执行者组内通过Ulysses通信交换注意力中间结果。
  - **延迟预算**：每个流式单元（160ms）内，思考者处理当前单元并解码上一单元，执行者组完成去噪并返回潜变量。模型端响应延迟保持约200ms（信号到信号），加上350ms双向网络预算，总交互延迟约550ms。
  - **公式/算法流程**：文字描述（无公式）：流式单元k → 思考者消费用户观测，产生新K/V切片 → 执行者rank接收切片，更新本地缓存，运行Ulysses上下文并行去噪 → 返回潜变量 → 思考者解码并输出。

## 3. 实验设计：使用了哪些数据集 / 场景，benchmark，对比了哪些方法

- **数据集/场景**：论文未使用公开基准数据集，而是使用模型生成的640×368对话片段进行**定性视觉观察**。评估场景包括近距离视频通话和中景场景锚定的智能体对话。
- **Benchmark**：未定义量化benchmark。作者称“遵循v0.1的响应边界测量”，但未给出具体数值对比结果（如PSNR、FVD等）。
- **对比方法**：未与任何其他方法进行直接的量化对比。论文引用了若干实时系统（如Doubao Realtime Voice、GPT-4o、Evi 3、StreamAvatar等），但仅作为背景，未进行实验对比。
- **实验覆盖**：仅报告了模型端延迟保持~200ms的运行时测量，无详细的消融实验或压力测试。

## 4. 资源与算力

- **文中提及**：思考器使用**单GPU**，执行器组为**多GPU**。未明确说明GPU型号、数量、训练时长或推理时具体硬件配置（如A100/H100等）。
- **缺失信息**：论文未提供训练所需的算力细节、预训练数据规模、以及部署时的具体GPU型号和数量（例如“8×A100”等常见描述）。作者团队来自阿里巴巴，但未披露工程细节。

## 5. 实验数量与充分性

- **实验数量**：极少。仅有一项延迟测量（与v0.1相同边界下保持~200ms），以及一组定性观察（观察生成视频的视觉稳定性、面部细节、姿势、手、物体等）。
- **充分性**：不充分。缺少以下内容：
  - 量化图像/视频质量指标（如SSIM、LPIPS、FVD、PSNR）。
  - 消融实验（如不同并行策略、不同rank数的性能比较、不同分辨率下的延迟/质量权衡）。
  - 对比实验（与v0.1或其他实时交互系统在同等条件下的公平对比）。
  - 主观用户研究（评估对话自然度、视觉清晰度等）。
- **客观性**：延迟测量遵循一致边界，但缺乏第三方验证。定性观察依赖作者自身判断，有主观偏向风险。

## 6. 论文的主要结论与发现

- **主要结论**：Wan-Streamer v0.2成功将输出分辨率从192×336提升至640×368，同时保持~200ms模型端延迟和~550ms总远程交互延迟（含350ms网络预算），从而支持更清晰的视频通话和场景锚定的中景智能体交互，姿势、目光、手部、物体和场景布局在实时对话中可读。
- **发现**：通过将高成本视觉潜变量生成迁移到Ulysses上下文并行执行器组，同时保持思考者为单GPU低延迟路径，可以在不降低交互节奏的情况下处理更大视觉流。音频潜变量序列短，无需分片，避免额外开销。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - 保持了原生流式的端到端因果建模，不拆分为级联子系统，使得感知、响应时机、语音、可见的倾听行为、同步视频响应均在一个因果交互状态内。
  - 思考器-执行器分离设计有效隔离了视觉生成成本，使额外硬件集中于视觉部分而不干扰延迟关键路径。
  - 使用Ulysses上下文并行来处理长序列的潜变量视频去噪，避免了KV缓存的全站复制，通信仅发生在执行器组内。
  - 音频潜变量因序列短而不分片，设计简洁高效。
- **创新点**：在实时交互场景中首次展示了分辨率提升（从192p到640p）而延迟不变。为高分辨率实时视频生成提供了工程模板。
- **实验设计之“亮点”**：延迟测量与v0.1保持相同边界，便于直接比较（尽管缺乏更多指标）。

## 8. 不足与局限

- **实验覆盖严重不足**：
  - 缺乏量化指标（图像质量、时序一致性、响应相关性等）。
  - 没有用户主观评价。
  - 没有与现有方法（如StreamAvatar、Hallo-Live等）进行公平对比。
  - 没有消融实验验证设计选择（如分片大小、rank数量影响）。
  - 延迟测量仅在理想网络假设下报告，缺乏网络抖动的鲁棒性测试。
- **偏差风险**：定性观察由作者自己进行，可能选择有利于展示的片段，存在选择性展示偏差。
- **应用限制**：
  - 仍需350ms双向网络预算，实际部署中网络延迟可能更高。
  - 未讨论模型在不同语言、口音、背景噪声下的表现。
  - 未评估模型安全性、偏见、幻觉等问题。
  - 训练数据未公开，复现困难。
- **资源与算力信息缺失**：无法评估部署成本或可扩展性。
- **方法局限**：思考器单GPU可能成为瓶颈，当输入模态更多或采用更高分辨率时可能无法保持延迟；执行器组内通信（Ulysses all-to-all）在rank增加时可能引入额外延迟。

（完）
